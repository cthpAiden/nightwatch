# AUDIT.md — Phase 1: six-axis audit (adversarially verified)

> Generated by a fan-out of 6 read-only axis-finders (one per axis) feeding an adversarial
> verifier per finding. **Every finding below was independently re-checked against the actual
> code by a second agent**; the verifier's adjusted severity / real-ness / safe-to-apply verdict
> is shown. Severities are the *verifier-adjusted* values, not the finder's original claim.

## Summary

- **46 raw findings, 44 confirmed real** by adversarial verification (2 rejected as invalid/misread).
- Confirmed by adjusted severity: **0 critical / 2 major / 31 minor / 11 polish**.
- **16 headless-verifiable** (safe to apply + confirm via SelfTest/IntegrityCheck/flow), **28 need human playtest**.

Legend: `[adjusted-sev | real? | safe? | verify]` — sev=verifier-adjusted severity, real=confirmed real,
safe=verifier says safe to apply, verify=H(eadless) or P(laytest).

## Performance  (8)

### 1. CCTV threat overlay rebuilds all nodes 4x/sec (free + re-create + lambda connect)
`[minor | real | safe | P]`  risk=medium  conf=high  **scripts/ui/camera_system.gd:256-275 (timer) -> 277-332 (_refresh_threats)**

- **Problem:** _process resets _refresh_t to 0.25 and calls _refresh_threats() 4 times/second (and again on every show_feed/_select). _refresh_threats() queue_free()s ALL _threat_host children, then for each on-screen threat allocates a fresh TextureRect AND a Button, news several Color objects, does CAM_DEPTH.get() (Dictionary lookup returning a Dict), and crucially connects a NEW lambda signal (tag.pressed.connect(func(): _c.tag_anomaly(tid))) every pass. That is continuous node churn + closure allocation + signal (re)connection while the monitor is open — the hottest UI path in the game. Most passes the threat set/positions are identical, so the teardown/rebuild is wasted.
- **Change:** Cache the per-side/per-threat TextureRect+Button by threat id in a dict. On refresh, only create nodes for newly-appeared threats, free nodes for departed ones, and update texture/modulate/position in place on the rest (instead of free+recreate). Move the lambda connection to creation time only. Keep the map-dot modulate reset (cheap) each pass. As a minimal first step, skip the entire rebuild when the set of visible threat ids + their textures + current_cam are unchanged since last pass.
- **Risk note:** Touches tag-anomaly wiring and reveal highlighting; a stale cache could show a wrong/dead figure, so the diff must invalidate on cam change and threat-list change.
- **Verify:** SelfTest/IntegrityCheck do not open the live monitor UI, so correctness of the on-feed figures needs human playtesting (open cameras, switch feeds, confirm tag still works and no ghost figures persist). Headless can only confirm the harnesses still pass (103/0, 36/0) i.e. no regression in non-UI logic.
- **Verifier:** File is at scripts/systems/camera_system.gd (not scripts/ui/). The finding is structurally correct: lines 272-275 trigger _refresh_threats() at 4Hz; lines 278-279 free all _threat_host children every pass; lines 305-332 allocate fresh TextureRect + Button per on-screen threat; line 331 connects a new lambda on every rebuild. The pattern is real. However, the severity is overstated. In practice most passes have zero threats on the currently-viewed camera (threats typically roam, and the player usually sees an empty feed), so the free+recreate work is usually O(0). When a threat IS on the current cam, the churn is 2 nodes freed + 2 allocated per threat per 0.25s — noticeable in a profiler but …

### 2. threat_at_door() O(n) scan called ~6x per frame in night loop
`[minor | real | safe | H]`  risk=low  conf=high  **scripts/systems/night_controller.gd:696-697, 585-586, 967 (x2 via _refresh_door_sprite L548-549); threat_at_door at threat_director.gd:53-57**

- **Problem:** Each _process frame, threat_at_door() (a full loop over director.threats) is invoked: twice in _update_via (L696-697), twice in _update_atmosphere as left_loom/right_loom (L585-586), and once per side inside _refresh_door_sprite (L967) called for LEFT and RIGHT (L548-549) — about 6 scans/frame of the threat list, recomputing the identical 'who is at which door' answer. _update_via even rescans the same thing _update_atmosphere already computed.
- **Change:** Compute left_loom/right_loom once at the top of _process (e.g. local threats_at_door[LEFT]/[RIGHT] = director.threat_at_door(side)) and thread those into _update_via, _update_atmosphere, and _refresh_door_sprite as parameters instead of having each recompute. Net: 2 scans/frame instead of ~6.
- **Risk note:** Pure read-side memoization within a single frame; ordering already has director ticking before these reads, so the value is stable for the frame.
- **Verify:** Run tools/SelfTest.tscn (NW_SKIP_TAPE=1 NW_NIGHT=6 godot --headless) expecting 103/0 and IntegrityCheck 36/0 — door/via/loom behavior is exercised, so identical pass counts confirm no behavioral change.
- **Verifier:** Finding is confirmed. threat_at_door() is called exactly 6 times per frame: 2x in _update_via (L696-697), 2x in _update_atmosphere (L585-586), and once each in _refresh_door_sprite for LEFT and RIGHT (L967 called from L548-549). Each call iterates the full director.threats array. The proposed fix — cache left/right results once at the top of _process and pass them as parameters — is safe and behavior-preserving. No invariants are touched (re-entrancy guards, audio cleanup, ending guards, scare-OFF gating are all unaffected). No cultural rules are implicated. No localization strings involved. The threat list is small (3-8 entries per night based on NightConfig.threat_levels), so actual CPU …

### 3. HUD warning string rebuilt with up to 4 tr() calls every frame + label re-set unconditionally
`[minor | real | safe | P]`  risk=low  conf=high  **scripts/ui/hud.gd:342-354 (_process)**

- **Problem:** _process computes the persistent warning by calling tr() (a localization table lookup) up to 4 times every frame (WARNING_LOW_POWER, WARNING_LOW_VIA, VENDOR_HOSTILE, POWER_OUT) and then assigns _warn.text = w and _warn_panel.visible every frame, even when w is unchanged. Assigning Label.text re-triggers TextServer shaping/relayout on the warning Label each frame. The inputs change rarely (state thresholds), so this is constant wasted work on a label that is usually empty.
- **Change:** Cache the last warning string in a member (e.g. _last_warn). Only build w when one of the cheap inputs changed, or simply guard the assignment: build w from the cheap comparisons (no tr) selecting a key, and only call tr()+set text+visible when the selected key differs from _last_warn_key. Skip tr() entirely when no condition is met.
- **Risk note:** Same displayed text; only adds a change-guard, low chance of a missed update if the key-diff is implemented correctly.
- **Verify:** The warning Label is a live HUD element not asserted by the harnesses; needs human playtest (drop power to red, vendor hostile, power-out) to confirm the banner still appears/clears. Headless run only confirms harnesses stay green.
- **Verifier:** The finding is real and accurately describes the code at lines 342-354 of scripts/ui/hud.gd. Every _process frame unconditionally executes up to 4 tr() calls (TranslationServer hash lookups) and unconditionally assigns _warn.text and _warn_panel.visible regardless of whether anything changed. No caching exists (_last_warn, _warn_key, etc. are absent from the file).

Severity is downgraded from major to minor. Godot 4's Label.set_text() and Control.set_visible() both short-circuit internally when the value is unchanged, so the Label relayout claim is not quite right — the property setter guards against redundant shaping. The real cost is 4 StringName hash lookups per frame, which is …

### 4. Desk-CRT shader 'lit' uniform written every frame though it changes only on power state
`[minor | real | safe | H]`  risk=low  conf=high  **scripts/systems/guard_room.gd:831-835 (_animate_screen)**

- **Problem:** _animate_screen runs every frame and calls _screen_mat.set_shader_parameter('lit', 1.0 if _powered else 0.0) unconditionally. _powered only flips on power-out/restore (rare), so this uniform write is redundant ~99.9% of frames. The 't' uniform genuinely must update each frame, but 'lit' does not.
- **Change:** Track the last value written (or just set 'lit' inside set_powered() and on _build, removing it from the per-frame path). Leave the 't' write as-is.
- **Risk note:** Single uniform; only risk is forgetting to set it on one power transition, mitigated by setting it in set_powered() where _powered changes.
- **Verify:** SelfTest exercises power-out; run headless expecting 103/0 (no logic touched). Visual CRT-dimming on blackout is cosmetic and would be confirmed in playtest, but correctness of the toggle is covered by ensuring set_powered() writes it.
- **Verifier:** Confirmed real. Line 835 of _animate_screen writes set_shader_parameter("lit", 1.0 if _powered else 0.0) every frame. _powered is declared true at line 82 and only flips inside set_powered() (line 941), which is called on rare power events. The shader uniform default is already 1.0 (shader line 300), so at startup no explicit set is even needed. set_powered() does not touch _screen_mat at all, so the per-frame write is the only thing keeping lit in sync. Safe fix: add _screen_mat.set_shader_parameter("lit", 1.0 if on else 0.0) inside set_powered() (guarded by if _screen_mat), and optionally initialize it in _build_monitor_screen after _screen_mat is created. Then remove line 835. The 't' …

### 5. Camera feed rect_aspect uniform written every frame regardless of window resize
`[polish | real | safe | P]`  risk=low  conf=high  **scripts/ui/camera_system.gd:261-262 (_process)**

- **Problem:** While the monitor is visible, _process recomputes _feed.size.x / _feed.size.y and calls set_shader_parameter('rect_aspect', ...) every frame. The feed rect only changes size on a window resize, so the division + uniform upload is redundant on the vast majority of frames.
- **Change:** Cache the last _feed.size (or last aspect) and only write the uniform when _feed.size changed since last frame. Alternatively drive it from a resize signal (size_changed/NOTIFICATION_RESIZED) and write once.
- **Risk note:** Aspect-fit is cosmetic and the guard is a trivial equality check; worst case a one-frame stale aspect after a resize.
- **Verify:** Aspect/COVER fit is a visual property not asserted by harnesses; confirm via playtest by resizing the window. Headless confirms no harness regression.
- **Verifier:** The finding is real: lines 261-262 of scripts/systems/camera_system.gd (not scripts/ui/camera_system.gd as cited — wrong path) do unconditionally recompute the aspect ratio and call set_shader_parameter('rect_aspect', ...) every frame while the monitor is visible. The code comment itself acknowledges it with "cheap, just a uniform write."

However, the claimed severity of 'minor' is overstated and should be 'polish'. The actual cost is: one float division + one ShaderMaterial.set_shader_parameter() call per frame, only while the camera monitor UI is open. This is negligible compared to the other work in the same _process body (_refresh_threats runs every 250 ms touching all children, the …

### 6. broadcast_view() loops all threats every frame even when monitor is closed
`[polish | real | care | H]`  risk=medium  conf=high  **scripts/systems/night_controller.gd:544 (call); threat_director.gd:99-101**

- **Problem:** _process calls director.broadcast_view(current_cam if monitor_open else '') every frame, which loops over every threat calling t.on_view(...). When the monitor is closed the argument is '' so on_view always resolves to false for all threats — a full per-threat loop whose result is invariant. Even when open it re-evaluates string compares for all threats each frame.
- **Change:** Skip the broadcast entirely when monitor_open is false and the previous frame was also closed (only broadcast the closing edge once). When open, only re-broadcast when current_cam changed or a threat moved — at minimum gate the every-frame call behind monitor_open with a single edge-broadcast on close.
- **Risk note:** The comment at L542-544 says the every-frame re-broadcast fixes a stale _viewing flag for threats wandering under a held view, so over-aggressive gating could reintroduce that bug; the safe change is to keep per-frame broadcast only while monitor_open and emit one final false-broadcast on close.
- **Verify:** SelfTest covers viewing/seen logic; run headless expecting 103/0. If the count drops, the gating broke the _viewing invariant and must be reverted to per-frame-while-open.
- **Verifier:** The finding is real: when monitor_open is false, broadcast_view passes "" every frame, making on_view set the same false for all threats each frame — invariant work. The closed-monitor case could be replaced with a single edge-broadcast on close.

However, the "when open, only re-broadcast on cam change or threat move" half of the proposal is NOT safe to apply as stated. The every-frame re-evaluation when the monitor IS open is explicitly documented (line 542-543 comment) as the intentional fix for stale _viewing flags when threats wander under a held camera. Switching to event-driven or change-only logic would require subscribing to Events.threat_relocated for all threats and carefully …

### 7. Per-frame Vector3/Color allocations in altar and threat-sprite animation loops
`[polish | real | care | P]`  risk=low  conf=high  **scripts/systems/guard_room.gd:773-801 (_animate_altar), 876-888 (_update_threat_sprites)**

- **Problem:** _animate_altar runs every frame and allocates fresh Vector3 objects per candle/smoke/tip (e.g. flame.scale = Vector3(...), node.position = s.base + Vector3(...), node.scale = Vector3.ONE * (...)) across 2 candles + 4 smoke puffs + tips; _update_threat_sprites likewise allocates per visible door sprite. These are small but constant GC/alloc pressure in the room's main animation tick, multiplied by element count every frame.
- **Change:** These are mostly unavoidable value assignments in GDScript (Vector3 is a value type but still constructed). The actionable win is to early-out the smoke/candle loops when not visible: skip the smoke loop body when _altar_lit is false (smoke alpha is forced 0 anyway, L800) and skip the whole _animate_altar inner work when _huong<=0 and not lit. Guard _update_threat_sprites is already cheap (skips invisible sprites). Apply the not-lit early skip rather than per-element micro-opt.
- **Risk note:** Only skips work that already produces invisible output; must keep flame.visible=false handling so guttered candles still hide.
- **Verify:** Altar visuals are cosmetic, not asserted by harnesses; confirm candle/smoke still animate when lit via playtest. Headless confirms harnesses stay green.
- **Verifier:** The finding is real: the smoke loop (lines 795-801) runs its full body every frame regardless of _altar_lit, performing trig + Vector3 construction for position and scale even when the altar is unlit. The candle scale assignment (line 785) is already safely gated under `if lit:`. The incense-tip loop (lines 792-793) also runs unconditionally but is just a float assignment.

However, the proposed fix ("skip the smoke loop body when _altar_lit is false") is NOT safe as stated. Line 800 sets node.modulate.a = 0.0 when not lit. If the entire loop body is skipped when not lit, the transition frame when _altar_lit flips from true to false would leave each smoke sprite at its last alpha value (up …

### 8. _desk_threat_tex() does a second full threat scan each frame while monitor open
`[polish | real | care | P]`  risk=low  conf=high  **scripts/systems/night_controller.gd:545-546 (call), 907-911 (_desk_threat_tex)**

- **Problem:** When monitor_open, _process calls room.set_desk_threat(_desk_threat_tex()) every frame; _desk_threat_tex loops director.threats looking for one whose current_location == current_cam. This duplicates iteration the camera_system feed-refresh and broadcast_view already perform, and runs every frame even though the mirrored threat/texture changes only when a threat enters/leaves the watched cam.
- **Change:** Have on_camera_changed / the director movement path set a cached _desk_threat_tex member, or only recompute when current_cam changed or a threat's location changed this frame, instead of an unconditional per-frame scan. Low urgency (n is tiny) — fold into the same memoized loom/threat-location pass from finding #2.
- **Risk note:** Texture mirror is cosmetic; a one-frame stale figure on the desk CRT is harmless.
- **Verify:** Desk CRT mirror is visual; confirm via playtest that the figure appears on the desk screen when a threat is on the operated cam. Headless confirms harness pass counts unchanged.
- **Verifier:** The finding is real: line 546 calls _desk_threat_tex() unconditionally every frame while monitor_open, and _desk_threat_tex() (lines 907-911) does a linear scan over director.threats each time. No caching exists. However, safe_to_apply is false for the proposed change: the threat texture can change in two independent ways — (a) camera switch (current_cam changes) and (b) a threat wanders into/out of the currently watched camera without any camera switch. The code comment at lines 542-544 explicitly documents case (b): "a threat that wanders under a held view (e.g. Oan hồn) is correctly seen/unseen." A cache keyed only on on_camera_changed would miss case (b) and silently show a stale …

## Graphics / Render  (7)

### 9. Tiled room textures bake in a radial vignette + corner darkening that repeats as a grid
`[minor | real | safe | P]`  risk=low  conf=high  **assets/art/room/wall.svg (lines 45-50), assets/art/room/floor.svg (lines 47-52), assets/art/room/door.svg (lines 64-69):wall.svg:45-50; floor.svg:47-52; door.svg:64-69**

- **Problem:** Each of these tileable surface textures ends with a full-texture radialGradient vignette (#0a0e14 darkening toward the texture corners). They are applied to the 3D room with uv1_scale > 1 (guard_room.gd:262 wall at 2x, :263 floor at 3x, :264 ceiling at 2x; door.svg used at 1x on the shutter). With StandardMaterial3D + uv1_scale>1 the UVs wrap (repeat is on by default), so the baked vignette repeats once per tile: a 2x-tiled wall shows a 2x2 grid of dark blobs, a 3x-tiled floor a 3x3 grid. This reads as obvious tiling seams / a checkerboard of shadows rather than continuous plaster or terrazzo, undermining the otherwise-strong grime work. Real per-surface darkening is already supplied by the room's lights + Environment fog/SSAO, so the baked vignette is both redundant and wrong once tiled.
- **Change:** Remove the corner-vignette rect+gradient from the THREE tiled textures only: delete wall.svg lines 45-50 (wallvig), floor.svg lines 47-52 (floorvig), and ceiling.svg's equivalent if present. Leave the non-tiled baked vignettes alone (camera SVGs at 1x, door.svg is 1x so its vignette does not repeat and can stay, or move it to a uv2 detail). Keep all grime/stain/crack/speckle detail untouched. Let the 3D lighting + env fog provide falloff. Optionally re-tint the now-uniform base so the wall doesn't read flatter (it won't: the broad tonal patches at wall.svg:5-8 already vary it).
- **Risk note:** Pure SVG edits to placeholder textures; only removes an additive dark overlay, cannot break geometry or shaders.
- **Verify:** IntegrityCheck/SelfTest only assert the SVGs exist+load, so they stay 103/0 and 36/0 after the edit (run both to confirm no regression). The visual fix (no repeating dark grid) needs an in-room screenshot via tools/shot.gd / NW_LOOK_* framing — human playtest.
- **Verifier:** Finding is real and independently confirmed. wall.svg:45-50 (wallvig), floor.svg:47-52 (floorvig), and ceiling.svg:49-54 (ceilvig) each bake a full-texture radialGradient corner vignette. guard_room.gd:262 applies wall at uv1_scale=2x, :263 floor at 3x, :264 ceiling at 2x — so these vignettes tile, producing a 2x2 grid on walls/ceiling and a 3x3 grid on the floor. The tiling artifact is a real graphical defect. door.svg:64-69 (doorvig) is applied at scale=1.0 (guard_room.gd:714) so it does NOT tile; the door vignette is harmless and can stay. The proposed fix — remove vignette elements from wall.svg, floor.svg, and ceiling.svg — is safe: it touches only SVG art assets, no GDScript logic, no …

### 10. Tiled 3D surfaces import with mipmaps disabled, causing minification shimmer/aliasing
`[minor | real | safe | H]`  risk=low  conf=high  **assets/art/room/wall.svg.import (line 26), and the matching .import for floor.svg / ceiling.svg:wall.svg.import:26 (mipmaps/generate=false)**

- **Problem:** wall.svg.import has mipmaps/generate=false. These textures are mapped onto large 3D walls/floor and tiled 2x-3x (guard_room.gd:262-264), so at the back of the room and down the dark hallways the high-frequency speckle/crack/grout detail minifies hard. Without mipmaps that produces crawling aliasing / sparkle as the camera pans (guard_room pans continuously), which on a CCTV-grade dark scene reads as noise rather than texture. This is exactly the case mipmaps exist for.
- **Change:** In wall.svg.import, floor.svg.import and ceiling.svg.import set mipmaps/generate=true (and re-import so the .ctex regenerates). Leave the camera-feed and UI SVGs as-is (they are screen-space, never minified). This is the surgical, asset-pipeline-only fix; no code change.
- **Risk note:** An import-flag flip on three placeholder textures; worst case is a one-time re-import and marginally larger .ctex.
- **Verify:** After editing the .import files, run godot --headless --import (or let SelfTest's headless boot re-import); then NW_SKIP_TAPE=1 NW_NIGHT=6 SelfTest must stay 103/0 and IntegrityCheck 36/0 (they load the textures, so a broken re-import would fail). Visual smoothness still wants a human pan-screenshot.
- **Verifier:** All three .import files confirm mipmaps/generate=false at line 26. The tiling is real: _mat() sets uv1_scale = Vector3(scale, scale, 1.0), with wall at 2.0x, ceiling at 2.0x, floor at 3.0x (guard_room.gd:262-264), and hallway reuses wall/floor at 2.0x (lines 688-689). The floor SVG contains 4px-wide grout lines and 2-3px speckle chips — exactly the high-frequency content that aliases without mipmaps. The camera pans continuously (_pan_speed is tracked on every frame). The proposed fix (set mipmaps/generate=true in the three room surface .import files only) touches no code, no localization, no invariants, no cultural rules, and no test harness. Camera-feed and UI .import files are correctly …

### 11. Camera monitor rebuilds all threat sprite+button nodes 4x/second unconditionally
`[minor | real | safe | P]`  risk=medium  conf=high  **scripts/systems/camera_system.gd (lines 272-332, 277-279):camera_system.gd:277-279 (queue_free loop) + :305-332 (recreate)**

- **Problem:** _process drives _refresh_threats every 0.25s (refresh_t reset at :274) and show_feed/_select also call it. _refresh_threats() first queue_free()s every child of _threat_host (:278-279) then recreates a TextureRect + a Button per on-feed threat (:305-332), assigning textures and reconnecting a lambda signal each time. Most refreshes have identical state (same threat, same cam), so this is steady node-churn + signal-reconnect + texture-rebind 4x/sec while the monitor is open, plus per-pass map-dot modulate writes (:283-290). It is wasteful and risks a 1-frame flicker of the tagged figure each rebuild.
- **Change:** Make _refresh_threats idempotent: cache the last-rendered (cam, threat_id, phase/texture, hostile, revealed) signature and early-return when unchanged; only free+rebuild the host children when the set of on-feed threats actually changes. Keep the existing depth/temperature/tag wiring; this is a guard around the same build code, not a redesign. The map-dot reveal modulate can stay every pass (cheap) or be folded into the same dirty check.
- **Risk note:** Touches the live tag/reveal hot path; a wrong dirty-check could leave a stale figure or drop the tag button, so the signature must include phase + reveal state.
- **Verify:** SelfTest doesn't open the monitor UI in a way that asserts node counts, so headless only confirms it still boots 103/0. Correct tag/figure refresh and absence of flicker need a human session opening cameras while a threat moves.
- **Verifier:** Confirmed by direct code inspection. _refresh_t initializes to 0.0 (line 10) so the first _process tick fires _refresh_threats() immediately; thereafter it fires every 0.25s (lines 272-275). show_feed() (line 243) also calls _refresh_threats() directly, and _select() calls show_feed(). Every call unconditionally queue_free()s all _threat_host children (lines 278-279) then recreates TextureRect + Button with fresh lambda signal connections (lines 305-332). The proposed fix — cache a (cam, threat_id, texture, hostile, revealed) signature and early-return when unchanged — is straightforward and safe. It touches no documented invariants (re-entrancy guards, audio loop cleanup, save atomicity, …

### 12. Office post-process grain/scanline shader runs full-screen every frame even when fully safe and when occluded
`[minor | real | safe | P]`  risk=low  conf=high  **scripts/systems/night_controller.gd (lines 157-178, 564-566):night_controller.gd:169 (base = 0.045 + 0.72*strength) + :565-566**

- **Problem:** The _post_layer canvas_item shader samples TIME-animated rand() per pixel every frame (psh at :158-173). Its alpha floor is base=0.045 even at strength 0 (:169), so it is never actually off — a full-screen noise pass runs continuously over the office. It also keeps running while the full-screen CCTV monitor (layer 10) completely occludes the layer-8 office, and while shop/cassette/pause panels are up, so the GPU pays for an invisible grain pass during those states.
- **Change:** Two surgical changes: (1) clamp the shader to truly zero out when safe — only add the noise/vignette term when strength > 0 (e.g. gate the grain/scan contribution behind strength, keeping at most a tiny static vignette), or set the post_rect.visible=false when dread<=0.001. (2) In _set_monitor and the shop/cassette/pause show paths, hide _post_layer (or post_rect) while a full-screen overlay covers the office, restoring it on return. Drive both from the existing _update_atmosphere strength so no new state is introduced.
- **Risk note:** Visibility toggling of a cosmetic overlay; the grain is non-gameplay, so a mis-timed hide is invisible at worst.
- **Verify:** SelfTest stays 103/0 (shader still compiles + scene boots). The perf win and 'office is clean when safe' look need a human frame-time/visual check; not asserted by either harness.
- **Verifier:** Finding is confirmed real. At night_controller.gd:169, when strength=0, base=0.045, so the fragment shader computes non-zero alpha (up to ~0.024 at screen edges) and writes it every frame — the noise pass is never fully off. No code anywhere sets _post_layer.visible=false during monitor open (_set_monitor at :864), shop, cassette, or pause overlay states; _post_layer lives at canvas layer 8, all overlays at layer 10. The GPU runs the TIME-animated rand() grain pass on every pixel even when completely occluded. Proposed fixes are safe: (1) setting _post_layer.visible=false when dread<=0.001 touches no invariant, cultural rule, audio path, save path, or test harness; (2) hiding _post_layer in …

### 13. Ma da's flood has no in-room visual — the office never shows rising water
`[minor | real | safe | P]`  risk=medium  conf=high  **scripts/systems/night_controller.gd (lines 299-309, 756-771) and scripts/systems/guard_room.gd (no water node):night_controller.gd:301-309 (_on_water_level_audio); guard_room.gd builds no flood plane**

- **Problem:** Ma da's flood is conveyed only by an audio loop (_on_water_level_audio :301-309) and a HUD meter (hud.gd water_bar). The 3D office — which already reactively dreads, shakes, and dims — shows nothing rising, so the signature Ma-da mechanic is invisible in the space the player is actually looking at. Co hồn, by contrast, gets a full-screen crowd overlay (:208-217), so the flood feels comparatively under-presented. This is a missing procedural visual, fully doable without assets.
- **Change:** Add a procedural water plane in guard_room: a thin translucent QuadMesh/PlaneMesh across the floor (y driven by a set_water_level(frac) API mirroring set_huong/set_dread) with a small spatial shader doing a cheap sine-ripple normal + cool blue-green emissive tint and a fresnel-ish edge, fed the same level NightController already emits on Events.water_level. Raise the plane's y and alpha with level. Reuse existing palette (cold blue-green night). Wire it next to set_dread in _update_atmosphere. No bitmap needed — geometry + shader only.
- **Risk note:** New transparent geometry in the 3D scene can introduce depth-sort/over-draw issues against the desk/altar; keep it unshaded+below props and test door-slam shake interplay.
- **Verify:** SelfTest must stay 103/0 (new shader compiles, set_water_level guarded for headless). The actual rising-water read is purely visual — human playtest on a Ma-da night.
- **Verifier:** Finding is confirmed real. guard_room.gd (1042 lines, fully read) builds no water plane and has no set_water_level API. Events.water_level signal (event_bus.gd:53) is emitted by ma_da.gd:29 on every tick; it is consumed only by night_controller.gd (audio loop, lines 301-309) and hud.gd (progress bar, lines 446-448). No 3D visual consumer exists anywhere in the codebase.

The cô hồn comparison holds: _crowd_overlay (a TextureRect full-screen image) is built at night_controller.gd:213-217 and driven by _update_crowd_overlay. Ma da has no equivalent visual in the room. The missing visual is at the procedural level and requires no bitmap assets.

Proposed change is safe: it would add a new …

### 14. Doorway threat billboards get no rim/colored light from the threat itself; reveal relies on flat modulate tint
`[polish | real | safe | P]`  risk=medium  conf=high  **scripts/systems/guard_room.gd (lines 697-711, 1024-1041) and scripts/threats/threat_base.gd (accent_color :21 unused for light):guard_room.gd:1032 / :1040 (hostile = Color(1.0,0.45,0.4) modulate)**

- **Problem:** The corridor-mouth Sprite3D is shaded and lit only by the doorway SpotLight (good), but a hostile threat is signalled purely by multiplying the sprite with a flat reddish modulate (:1032,:1040). There is no light cast by the figure, so threats like Ma trơi (whose whole identity is cold wisp-light) and a hostile vendor read as a tinted decal, not a glowing presence. ThreatBase already carries accent_color (:21) that is never used for any lighting. This is a cheap, high-atmosphere procedural win.
- **Change:** Give each doorway a small OmniLight3D co-located with the threat sprite (_threat_base_pos), off by default, energized only while a threat stands there using threat_base.accent_color (cold teal for ma_troi, hot red when hostile, warm for vendor). Pulse its energy in _update_threat_sprites with the existing sway phase so the silhouette is back-/rim-lit and casts a faint colored wash into the corridor. Drive it from show_threat/refresh_threat_visibility which already know tex+hostile. Energy 0 when no threat so it never leaks light.
- **Risk note:** An extra dynamic shadow-casting-capable light per doorway adds a little cost and could double-count with the door spotlight; keep shadows off and energy modest.
- **Verify:** SelfTest stays 103/0 (lights are inert headless). The rim-light/reveal read is visual — human check at a lit doorway with a threat present.
- **Verifier:** Finding is confirmed real. In guard_room.gd:1032 and :1040, show_threat/refresh_threat_visibility apply only a binary hardcoded modulate (hostile Color(1.0,0.45,0.4) vs. neutral Color(0.72,0.74,0.74)), with no per-threat color variation and no dynamic light. ThreatBase.accent_color (threat_base.gd:21) is set per-threat in threat_director.gd:29 via meta.get("accent",...) but is never referenced by any rendering path. The _update_threat_sprites function (guard_room.gd:876) does sway animation but adds no lighting pulse either. The proposed fix (add an OmniLight3D per doorway, energy=0 by default, energized from accent_color while a threat is present, pulsed with the sway phase) is purely …

### 15. Incense smoke billboards are a fixed 4-puff loop with no jitter/turbulence and a single texture
`[polish | real | safe | P]`  risk=low  conf=high  **scripts/systems/guard_room.gd (lines 462-475, 794-801) and assets/art/room/smoke_puff.svg:guard_room.gd:465-475 (4 puffs) + :795-801 (rise anim)**

- **Problem:** The altar smoke is 4 Sprite3D puffs cycling on a deterministic fmod(_t) rise with only a sin() x-wobble (:799) and a single radial-gradient smoke_puff.svg. All four share phase offsets 0.85 apart, so the column reads as a repeating, mechanical pump rather than drifting incense — the one continuously-visible 'living' atmosphere element on the sacred altar (which the design foregrounds). Particle turbulence here is pure procedural polish, no assets.
- **Change:** Two cheap upgrades, both code-only: (1) add per-puff RNG seeds to phase, horizontal drift amplitude, and max alpha so the four strands desync and curl differently; widen the x-drift and add a second-harmonic sin for a lazy S-curve. (2) Optionally swap the 4 hand-managed Sprite3D puffs for a GPUParticles3D with a ParticleProcessMaterial (upward velocity + small turbulence + scale-over-life + alpha-over-life), keeping smoke_puff.svg as the particle texture and gating emission on _huong/_altar_lit. Keep alpha tied to _huong and OFF when guttered (:800) — preserves the mechanic.
- **Risk note:** Self-contained cosmetic on a non-gameplay element; the _huong/_altar_lit gating that the ritual depends on is preserved.
- **Verify:** SelfTest stays 103/0 (particles/sprites inert headless, smoke_puff.svg still loads). The drift quality is visual — human look at the lit altar.
- **Verifier:** Finding is real and accurately described. guard_room.gd:465-475 creates exactly 4 Sprite3D puffs sharing one smoke_puff.svg (a plain radial-gradient circle, confirmed), with deterministic phase offsets of float(i)*0.85 (0, 0.85, 1.70, 2.55). The rise animation at :795-801 uses a single sin() x-wobble with amplitude 0.06*ph - noticeably mechanical. No RNG, no second harmonic, no per-puff amplitude variation. The _huong and _altar_lit gating is correctly implemented (line 800) and any proposed change must preserve these. The proposed additions (per-puff RNG seeds seeded at scene load for phase/drift/alpha, a second sin harmonic for S-curve drift) are pure procedural code changes with no new …

## Audio  (7)

### 16. No stereo panning anywhere — left/right threats and doors are spatially indistinguishable
`[minor | real | care | P]`  risk=medium  conf=high  **scripts/core/audio_manager.gd:38-40, 137-147, 167-180; consumers e.g. scripts/systems/night_controller.gd:600-620, scripts/threats/threat_base.gd:159**

- **Problem:** Every cue plays through a plain mono AudioStreamPlayer (pool, loops, music). There is no AudioStreamPlayer2D/3D and never a call to set a pan. The game has a hard physical left/right axis (GameEnums.Side.LEFT/RIGHT doors, threat_at_door(LEFT/RIGHT), left_loom/right_loom at night_controller.gd:585-586,600-620). The only side cue is a pitch nudge: breathing is pitched 0.92 for a left loom vs 1.0 otherwise (night_controller.gd:603,607) and ma_da water_call is pitched by direction in ma_da.gd:69 — pitch, not position. A door knock/arrival sting (threat_base.gd:159) gives no directional information at all, so the player cannot hear which door is threatened, which the design explicitly wants them to discover. This is the single biggest spatialization gap.
- **Change:** Add an optional pan argument to play_sfx and a set_loop_pan helper, implemented by routing through Godot's panning. Simplest within the mono pipeline: give AudioStreamPlayer a per-call pan via a stereo bus is not possible directly, so either (a) switch the SFX pool players to AudioStreamPlayer (keep) but add 'pan' by setting `p.set('pan', x)` is unavailable — instead create three SFX buses/sends or use AudioStreamPlayer2D positioned by side. Concretely: convert the one-shot pool and the door/threat loops to AudioStreamPlayer2D children of a 2D listener at screen center, and position them at x = -200 (LEFT) / +200 (RIGHT) / 0 (center) based on the side already known at the call site. Pass side through play_sfx for door_slam/door_creak (guard_room.gd:1006), the arrival sting (threat_base.gd:159), breathing/shutter_strain loops (night_controller.gd:600-620), and water_call (ma_da.gd). Keep VERB reverb send so panned scares still bloom.
- **Risk note:** Touches the central play path and loop lifecycle; AudioStreamPlayer2D needs a listener and correct bus routing, and the death/scene-change loop-cleanup invariant must still hold for the new players.
- **Verify:** Needs human playtesting with headphones: trigger a left-door vs right-door arrival and confirm the knock/breath is audibly on the correct side. SelfTest only asserts no crash, not stereo image. Headless can assert the pan/position value is set on the player, but not the perceived result.
- **Verifier:** Finding is confirmed real. All AudioStreamPlayer instances in audio_manager.gd (pool at line 38, loops dict at line 39, music player at line 40) are plain mono players — zero occurrences of AudioStreamPlayer2D/3D or any pan property in the entire codebase. The threatening_side is available at every call site (threat_base.gd:150 sets it 9 lines before line 159 fires the approach_sfx; guard_room.gd:1006 has `side` in scope; night_controller.gd:600 receives left_loom/right_loom as args). The pitch-nudge workaround (0.92 for left) is confirmed at night_controller.gd:603. Design comment at threat_base.gd:156-158 explicitly states direction is intentionally left to visual doorway lights, not …

### 17. REDUCED accessibility tier does not lower stinger/jumpscare-substitute loudness — only the jumpscare WAV is scaled
`[minor | real | care | H]`  risk=low  conf=high  **scripts/core/audio_manager.gd:160-164; scripts/core/settings_manager.gd:83-87; consumers night_controller.gd:1033,1066,1183,1280, threats/*.gd, vendor.gd:106**

- **Problem:** Settings.scare_volume_scale() returns 0.55 for REDUCED, 1.0 for FULL, and it is consumed ONLY in play_jumpscare (audio_manager.gd:161-164). Every other scare-grade cue — pre_scare (night_controller.gd:1033), the no-image sting_breath substitute (1066), sting_breath startle (1183), candle_gust + sting_breath (1279-1280), sting_metal mask-drop (vendor.gd:106), sting_low/sting_rise/sting_metal in the threat scripts — is played at the same fixed dB on FULL and REDUCED. The accessibility contract is that REDUCED is a gentler tier; right now a REDUCED player still gets full-loudness dread stings and the full-loudness pre-scare swell, so the audio half of the tier is largely a no-op. The visual side already honors REDUCED (shake halved, no jitter, soft strobe at night_controller.gd:1056-1062).
- **Change:** Apply scare_volume_scale() as a dB offset to scare-grade cues. Cleanest: in play_sfx, when bus == VERB_BUS (the convention for scares/stingers), add `volume_db += linear_to_db(maxf(0.001, Settings.scare_volume_scale()))` and early-return when scale<=0 (OFF). That single change covers pre_scare, all sting_*, water_call, whisper-on-VERB, and the mask-drop, since they already route to VERB_BUS. Verify pre_scare and the sting_breath substitute paths route via VERB (they do: night_controller.gd:1033,1066).
- **Risk note:** One central, additive dB tweak gated on the existing VERB convention; does not change FULL behavior (scale 1.0 = 0 dB) and respects the OFF early-return already used for jumpscare.
- **Verify:** Headless: set Settings.scare_intensity = REDUCED, call Audio.play_sfx('sting_low', 0.0, 1.0, Audio.VERB_BUS), and assert the returned player's volume_db == linear_to_db(0.55) (~-5.2 dB). Confirm SelfTest (103/0) and IntegrityCheck (36/0) still pass. FULL must yield 0 dB unchanged.
- **Verifier:** Finding is real and confirmed. scare_volume_scale() is consumed only in play_jumpscare (audio_manager.gd:164); every other VERB_BUS scare cue — pre_scare (nc:1033), sting_breath no-image path (nc:1066), sting_breath startle (nc:1183), sting_breath candle-gust (nc:1280), sting_metal mask-drop (vendor.gd:106), sting_metal quy_nhap_trang.gd:73, sting_rise ma_da/ma_troi/phone_system, sting_low ma_troi/door-arrival, water_call ma_da, whisper co_hon — all play at hardcoded dB regardless of REDUCED. Visual REDUCED handling (shake halved, jitter skipped, soft strobe) at nc:1056-1062 is already in place. The audio half is indeed a near-no-op for REDUCED. Severity adjusted to minor (not major) …

### 18. Master chain double-stages distortion: 3.5:1 compressor + limiter with 2 dB soft-clip on already peak-0.9 pre-normalized cues — jumpscare slamming
`[minor | real | safe | P]`  risk=low  conf=high  **scripts/core/audio_manager.gd:80-92**

- **Problem:** The master limiter is set with soft_clip_db = 2.0 (audio_manager.gd:91). Combined with the -14 dB / 3.5:1 compressor in front (82-88) and source WAVs that gen_sfx.py already normalizes to peak 0.9 and soft_clips (e.g. jumpscare save(...,peak=0.9) at gen_sfx.py:333, soft_clip in stinger/door_slam/etc.), loud stacked hits (jumpscare on VERB + duck recovery + sub) drive 2 dB of intentional soft-clip saturation into an already-hot, already-soft-clipped signal. The result is audible harmonic grunge/grit on the biggest moment rather than a clean wall. soft_clip_db on AudioEffectLimiter adds tanh-style saturation BELOW the ceiling, so it is always coloring, not just catching peaks.
- **Change:** Lower the limiter soft_clip_db from 2.0 to ~0.0–0.5 so it acts as a true brickwall catch rather than a saturator, and/or ease the compressor ratio from 3.5 to ~2.5 and raise threshold from -14 to ~-10 so it stops gain-pumping the bed. Keep ceiling_db = -0.6. This is a mixing-stage change only (procedural pipeline), no asset re-record.
- **Risk note:** Pure bus-effect parameter change in _ensure_buses; reversible and isolated, no gameplay or invariant impact.
- **Verify:** Headless can assert the new parameter values are set on the Master limiter/compressor, but the audible improvement (cleaner jumpscare, less pumping) requires human listening. SelfTest will confirm no regression/crash.
- **Verifier:** Finding confirmed against actual code. audio_manager.gd lines 89-92: limiter has soft_clip_db=2.0 with ceiling_db=-0.6, meaning tanh saturation applies from -2.6 dB down. gen_sfx.py line 120 confirms soft_clip() is pure tanh; line 333 confirms jumpscare.wav is soft_clip(out) normalized to peak=0.9. The double-saturation path is real. Severity downgraded from major to minor: the compressor attack_us=18000 (18ms) is slow enough that sharp transients largely pass through, but repeated hits still accumulate tanh coloring. The proposed fix (lower soft_clip_db to 0.0-0.5, optionally ease compressor to ratio=2.5/threshold=-10) is safe: it only changes procedural mixing parameters in …

### 19. static_loop, breathing, whisper, fluorescent_hum have no seam-masking — possible loop click / abrupt gap
`[minor | real | safe | P]`  risk=low  conf=high  **tools/gen_sfx.py:253-255 (static_loop), 291-300 (breathing), 228-240 (fluorescent_hum), 302-312 (whisper)**

- **Problem:** The newer ambient loops (drone_tension, ambience_sub, water_loop, shutter_strain, incense_bed) were deliberately built seam-clickless (comments at gen_sfx.py:500-513, 586-598, 634-651, 722-739). But static_loop is a raw 2.0s bandpassed noise slice (253-255) with no amplitude taper at the boundary, so the random sample at t=2.0 vs t=0 produces a discontinuity → periodic tick every 2s when looped (static_loop is in LOOPING, audio_manager.gd:28). breathing (291-300) ends near silence via ADSR release so its seam is benign, and fluorescent_hum is currently unused, but static_loop is actively looped in cassette_ui/tape_archive/night camera-up. Its harmonic content (random noise) makes any boundary step audible as a click against the quiet CRT bed.
- **Change:** In static_loop(), multiply the buffer by a gentle cosine seam window like the others — e.g. for each sample apply `0.85 + 0.15*(0.5-0.5*cos(2*pi*i/n))` so amplitude is continuous (equal) at both ends, OR generate it as a sum of band-limited sines/looped grain so start==end. Cheapest: taper the first and last ~5 ms with a raised-cosine so the boundary step is masked. Re-run python tools/gen_sfx.py and re-commit the WAV.
- **Risk note:** Single synthesis function; deterministic seed unchanged, only the envelope of one placeholder WAV changes.
- **Verify:** Human listening on a looped static_loop to confirm the periodic tick is gone. Headless can only confirm the regenerated WAV still loads and SelfTest passes.
- **Verifier:** The static_loop finding is confirmed real and active. gen_sfx.py:253-255 generates a raw 2.0s bandpassed noise buffer with no boundary handling — the last sample is a random noise value that almost certainly differs from the first, producing a discontinuity (audible click) every 2 seconds when looped. Confirmed active: static_loop is in LOOPING (audio_manager.gd:28) and started in cassette_ui.gd:58, tape_archive_screen.gd:65, night_controller.gd:877.

The newer ambient loops (water_loop:644, shutter_strain:730, incense_bed:750) all use `seam = 0.5 - 0.5*cos(2*pi*(t/dur))` to force amplitude to zero at boundaries; drone_tension and ambience_sub use mathematically seamless sine frequencies. …

### 20. Verb reverb wet send is global and untouched by the duck, so it does not respect the SFX slider correctly during a death duck
`[minor | real | safe | H]`  risk=low  conf=medium  **scripts/core/audio_manager.gd:94-98, 201-219**

- **Problem:** apply_volumes sets the Verb bus volume to track the SFX slider (audio_manager.gd:98), but duck() only drops MUSIC_BUS and SFX_BUS (202), intentionally leaving Verb full so scares punch through. That is the documented design. However, the Verb bus level is the SFX slider level, while the dry SFX bus is ducked ~16-26 dB below the slider — so during a duck the reverb tail of a scare is up to 26 dB LOUDER relative to the (ducked) dry mix than the synthesis intended, and on a quiet REDUCED/low-SFX setting the wet tail can dominate. There is no separate Verb-tracking that accounts for the scare scale either (ties to finding #2).
- **Change:** Either (a) accept the design but cap the Verb send so it cannot exceed the un-ducked SFX level by more than the intended wet amount (set Verb a few dB below SFX in apply_volumes), or (b) fold the REDUCED scare scale into the Verb level so the wet bloom also gets gentler on REDUCED. Lowest-risk: in apply_volumes set the Verb bus a fixed ~3 dB under sfx_volume so the surviving tail sits behind, not over, the recovered mix.
- **Risk note:** One line in apply_volumes; preserves the punch-through behavior, just trims relative tail loudness.
- **Verify:** Headless: after apply_volumes, read AudioServer.get_bus_volume_db(Verb) and assert it is ~3 dB below the SFX bus. Confirm both harnesses stay green.
- **Verifier:** The code confirms the factual basis: apply_volumes (line 98) sets Verb to sfx_volume, and duck() (line 202) only drops MUSIC_BUS and SFX_BUS, leaving Verb untouched — both explicitly documented as intentional design. However, the finding's framing is imprecise in a meaningful way: the Verb bus is NOT a parallel wet-send; it is a full playback bus for the jumpscare with reverb as an INSERT effect (dry=0.92, wet=0.32 at lines 73-74). The jumpscare audio on the Verb bus is primarily dry signal plus some insert reverb, not a reverb-only tail. The "26 dB louder reverb tail" claim overstates the issue — the dominant energy is the dry jumpscare itself (0.92 dry weight), which is intended to punch …

### 21. clock_tick / footstep_wood / approach_soft are thin and clicky — minimal body, very short decay tails
`[minor | real | safe | P]`  risk=low  conf=high  **tools/gen_sfx.py:166-174 (clock_tick), 395-399 (footstep_wood), 626-631 (approach_soft)**

- **Problem:** clock_tick is a 0.006s exp_decay noise transient plus a tiny 0.3-gain 160 Hz thud (gen_sfx.py:170-173); at the per-hour cadence and -10 dB it reads as a bare click with no mechanical 'tock' resonance. footstep_wood (395-399) is a single 0.03s-decay 120 Hz sine + short noise — no floorboard creak/resonant tail, so it sounds like a soft tap, not wood. approach_soft (626-631) is a 0.02s-decay tap, almost a click. These are the cues that recur most (clock every hour, footstep per move), so their thinness is disproportionately noticeable.
- **Change:** Add a short resonant body + slightly longer decay: for clock_tick, layer a damped 2-3 partial 'tock' (e.g. 800/1900 Hz with tau~0.03) under the transient; for footstep_wood, add a low-Q resonant wood mode (~180 Hz and ~520 Hz sines, tau~0.06) and a faint creak via bandpassed noise tau~0.05; for approach_soft, extend the sine decay tau to ~0.05 and add a soft sub thump so the 'tap from the dark' has weight. Re-run gen_sfx.py.
- **Risk note:** Additive layering in three isolated synth functions; deterministic seed preserved, only placeholder WAVs change.
- **Verify:** Human listening to confirm the tick/step gains body. Headless only confirms regenerated WAVs load and harnesses pass.
- **Verifier:** All three synthesis blocks confirmed as described. clock_tick: 0.006s noise decay + 0.01s thud decay in a 0.06s buffer — effectively a bare click. footstep_wood: 0.03s / 0.02s decays in a 0.22s buffer — content evaporates far before the buffer ends. approach_soft: 0.02s / 0.01s decays in a 0.5s buffer — nearly inaudible tap. The proposed layering (resonant partials, extended tau values, sub thump) is purely additive synthesis parameter work inside gen_sfx.py, which is the permitted procedural audio pipeline. No impact on SelfTest.tscn or IntegrityCheck.tscn (those test game logic, not .wav content). No localization strings affected. No cultural/sacred items involved. No documented …

### 22. dong-dao / dread motif is absent — no melodic eerie folk motif in the procedural set
`[minor | real | safe | P]`  risk=medium  conf=high  **tools/gen_sfx.py:497-513 (drone_tension), 555-565 (ambience_*), 176-183 (clock_chime)**

- **Problem:** The brief calls out 'the eerie dong-dao motif' (a Vietnamese children's-rhyme melody, a classic folk-horror device). The tension bed is only a detuned drone (drone_tension, gen_sfx.py:497-513) and the ambience is only crickets (555-565) — there is no pitched melodic motif anywhere in the synthesis set, so the strongest culturally-grounded horror audio idea is unrealized. clock_chime (176-183) is the only pitched recurring cue and it is a neutral bell.
- **Change:** Add a new procedural cue, e.g. dong_dao_motif(): synthesize a short, slightly-detuned, music-box/celesta-timbre melodic phrase (a handful of inharmonic bell partials like offering_bell but sequenced into a simple pentatonic-ish 4-6 note motif), pitched slowly drifting flat, with a long reverb-friendly decay tail, intended to be played sparingly on late nights (gate it behind night_index and route to VERB). Add it to SFX_NAMES in audio_manager.gd and trigger it rarely from the director/late-night branch. Keep it authentic and non-desecrating (a nursery-rhyme melody, not sacred chant). Stays procedural-only.
- **Risk note:** New asset + a new trigger site; must be gated so it does not over-play and must respect scare-off (route via VERB + scare scale per finding #2). Cultural framing must stay respectful per DESIGN.md.
- **Verify:** Human listening for the motif's eeriness and pacing; design review for cultural fit. Headless confirms the new WAV loads, is in SFX_NAMES, and harnesses pass.
- **Verifier:** Finding is confirmed real. gen_sfx.py has no dong_dao motif function — verified by exhaustive def listing. DESIGN.md lines 513-516 explicitly names the "eerie dong-dao" as a signature motif and a core audio requirement. drone_tension (497-513) is a sub-bass drone, ambiences (555-565) are cricket-only, clock_chime (176-183) is a neutral bell — none serve as a melodic folk cue. The proposed change (synthesize an original pentatonic music-box phrase using inharmonic partials, like offering_bell but sequenced) is safe: (1) fully procedural, no real-asset constraint violated; (2) using an original melody avoids reproducing copyrighted rhyme lyrics, satisfying DESIGN.md cultural rule and the …

## Gameplay loop & readability  (8)

### 23. Threat at the doorway is invisible unless that side's light is already ON, with no persistent 'turn on the light' cue
`[major | real | safe | P]`  risk=low  conf=high  **scripts/systems/guard_room.gd:1041 (refresh_threat_visibility), 1033 (show_threat)**

- **Problem:** The 3D doorway billboard is only drawn when the light on that side is on: spr.visible = has_threat and _light_on.get(side, false). The arrival gives a one-shot flash_side() pulse on the light BUTTON (night_controller.gd:282-283) and a 2.6s THREAT_NEARBY toast ('check the doorway lights'). But the pulse is a ~0.25s blink (hud.gd:406-414) and the toast scrolls away; after that, a player who didn't flick the light sees an empty lit-or-dark doorway with a threat silently draining vía (via_drain_at_door, threat_base.gd:175) and reaches the kill with no on-screen indication of WHICH side or even THAT something is there. The directional read (which door) depends entirely on catching a sub-second pulse or remembering the arrival sting pitch.
- **Change:** Make the threatened side legible for as long as a threat looms, not just on arrival. Cheapest: in hud._process, while director.threat_at_door(side) is non-null, hold the corresponding light button at a steady amber tint (or pulse it slowly) instead of the one-shot flash_side; clear it when the door is empty. This reuses existing state (the controller already calls _refresh_door_sprite every frame at lines 548-549) and adds a persistent directional cue without revealing the threat identity (preserves the FNAF-style 'flick the light to see' tension).
- **Risk note:** HUD-only tint change driven off existing per-frame door state; no gameplay numbers or invariants touched.
- **Verify:** manual playtest: let a path threat (Ông kẹ) reach a door without touching the light; confirm the threatened side is continuously indicated on the HUD
- **Verifier:** Confirmed real. guard_room.gd:1033 (show_threat) and :1041 (refresh_threat_visibility) both gate spr.visible on _light_on.get(side, false), so a threat is invisible when its side's light is off. night_controller._process calls _refresh_door_sprite every frame (lines 548-549), but that just re-asserts the same gate — no persistent directional cue is produced. The arrival gives only a 0.25s flash_side tween (hud.gd:412-414) and a scrolling toast. After those pass, a player who left the light off sees no indication that anything is looming.

The proposed fix (hold a steady amber tint on the light button while threat_at_door(side) is non-null) is architecturally sound and safe: purely additive …

### 24. SHAKEN vía has no persistent warning text — only CRITICAL does — so the first danger tier reads as silent
`[minor | real | safe | P]`  risk=low  conf=high  **scripts/ui/hud.gd:344-354 (persistent warning block)**

- **Problem:** The persistent _warn line only surfaces at via_state == CRITICAL (line 346, frac<=0.15) and low power. The SHAKEN tier (via_state.SHAKEN, frac<=0.4, set in night_controller.gd:715) ramps the heartbeat and tints the vía bar (hud.gd:380-383) but gives no words. Since the meter threats (cô hồn/ma da) bleed you down silently and the only textual escalation is the CRITICAL red, a player can slide from comfortable to nearly-dead with the only explicit warning arriving at 15% — a narrow window to react given the bleed coefficients deepen near full (co_hon.gd:29, ma_da.gd:48).
- **Change:** Add a softer SHAKEN-tier persistent warning between the normal and CRITICAL states in hud.gd:344-354 (e.g. if via_state == SHAKEN: w = tr('WARNING_VIA_SHAKEN')), using an amber tint rather than COL_DANGER. Add the WARNING_VIA_SHAKEN string to strings.csv (vi+en) and re-import. Keep CRITICAL taking precedence (it is checked after, so order already favours it).
- **Risk note:** Pure additive HUD warning text; requires a strings.csv add + .translation re-import but no logic/invariant change.
- **Verify:** manual playtest: let vía drop into the 16-40% band, confirm an amber warning shows and upgrades to red below 15%
- **Verifier:** Confirmed real. hud.gd:342-354 shows four warning conditions; none covers ViaState.SHAKEN. The SHAKEN tier (frac 0.15-0.40, set in night_controller.gd:710-716) only gets ambient signals — amber bar tint (hud.gd:381), pink vignette (night_controller.gd:319-320), heartbeat ramp — but no persistent text. The 25-percentage-point silent window between SHAKEN onset and CRITICAL is wide relative to the bleed rates (co_hon up to 0.11*(crowd-60)*delta, ma_da 0.05*(flood-70)*delta). Proposed fix: add `if _c.via_state == GameEnums.ViaState.SHAKEN: w = tr("WARNING_VIA_SHAKEN")` before line 346 in hud.gd so CRITICAL overwrites it (correct precedence). Also requires adding WARNING_VIA_SHAKEN to …

### 25. Bell cooldown (18s) is invisible — the button gives no disabled/cooldown feedback, so presses during cooldown silently no-op
`[minor | real | safe | P]`  risk=low  conf=high  **scripts/ui/hud.gd:141-143 (_bell_btn build), 1325-1339 (request_ring_bell)**

- **Problem:** request_ring_bell early-returns silently when _bell_cd>0 (night_controller.gd:1326) — no sound, no toast, no button state change. The bell button (_bell_btn, hud.gd:141) never reflects its 18s cooldown (_bell_cd_max, line 66). The incense button, by contrast, surfaces its finite count and greys out when empty (hud.gd:477-481). A player who taps the bell during cooldown (the natural reaction when a rusher is pressing) gets zero feedback and may believe the input or the bell itself is broken, undermining trust in a key positioning tool.
- **Change:** Give the bell button cooldown feedback. Emit a bell_cooldown signal (or reuse a notify) with remaining fraction, and in hud disable/dim _bell_btn while cooling and restore it when ready — mirror the incense button's modulate pattern at hud.gd:481. Minimal version: in request_ring_bell when _bell_cd>0, play a soft denied blip and a brief button shake so the press is acknowledged.
- **Risk note:** HUD/feedback-only; the bell's gameplay effect and cooldown value are unchanged.
- **Verify:** manual playtest: ring the bell, immediately press again, confirm the button shows it is on cooldown and the second press is acknowledged
- **Verifier:** Confirmed real. The bell button (_bell_btn, hud.gd:30, 141-143) has zero cooldown feedback. night_controller.gd:1326 silently early-returns when _bell_cd>0 — no signal emitted, no button state change. The Events bus (event_bus.gd) has no bell-specific signal and hud.gd's _process loop (line 329) does not poll _bell_cd. The incense button comparison is exact: incense_changed signal drives _on_incense() which modulates _incense_btn (hud.gd:477-481); no equivalent exists for the bell. Proposed fix is safe: polling _c._bell_cd in _process to dim/restore _bell_btn via modulate (mirroring the incense pattern at hud.gd:481) touches no invariants — no re-entrancy guard, no save atomicity, no …

### 26. Night-1 tutorial freezes the sim and the clock, but nothing tells the player the lesson is paused / how to finish
`[minor | real | safe | P]`  risk=low  conf=high  **scripts/systems/night_controller.gd:519-522 (_process tutorial freeze), 448-465 (_check_tutorial)**

- **Problem:** During the tutorial the whole sim is frozen (clock, power, threats — _process returns early at line 522) and only the persistent banner naming the current step is shown (hud.set_tutorial_prompt). There is no indication that time is stopped, so a cautious player may rush, and conversely the banner only names ONE step at a time with no progress sense (e.g. 'step 3 of 5'). The LOOK step completes on get_pan_speed()>0.5 (line 451) which can trigger accidentally before the player reads the prompt, skipping the lesson's intent. Onboarding clarity is thin for the one moment it matters most.
- **Change:** Two low-risk clarity adds: (1) show step progress in the banner (e.g. prefix '(2/5) ' using _tut_step / TUT_STEPS.size() in set_tutorial_prompt at hud.gd:282), and (2) require a deliberate look — raise the LOOK completion threshold or require the pan to be sustained briefly so an incidental mouse twitch does not auto-advance step 0 (line 451).
- **Risk note:** Tutorial-only, gated behind the non-headless Night-1 path; NW_SKIP_TAPE tests bypass the tutorial so harnesses are unaffected.
- **Verify:** manual playtest: run Night 1 fresh, confirm step progress is visible and the LOOK step needs an intentional look
- **Verifier:** Both observations are confirmed by the code. (1) The `_process` early-return at line 522 is real and intentional (documented in the comment), and there is no "time is paused" indicator shown to the player — hud.set_tutorial_prompt only shows the step key translated via tr(). (2) Step 0 (TUT_LOOK) completes on a single-frame check `get_pan_speed() > 0.5` where `_pan_speed = absf(_yaw - prev_yaw) / delta`. Any mouse movement into the window on startup can exceed 0.5 in one frame, silently skipping the lesson. Both issues are real and not already handled. Proposed fix (1): Adding a step-progress prefix is safe for the GDScript side, but the bilingual constraint says player-facing text must be …

### 27. Path-threat counter prompt (COUNTER_ONG_KE etc.) only fires the instant the threat is already at the door — too late to act calmly
`[polish | real | safe | H]`  risk=medium  conf=high  **scripts/systems/night_controller.gd:286-288 (_on_threat_at_door first-contact teach)**

- **Problem:** For rusher/path threats the contextual counter hint is emitted only on first arrival AT the door (_first_door gate, lines 286-288), at the same moment the attack timer (threat_base attack_time, default 6s) has started and vía is already draining. The meter threats teach earlier — at 60-70% meter, before the kill window (co_hon.gd:24-27, ma_da.gd:44-47). Path threats get no such lead time, so the first time a player meets Ông kẹ/Quỷ nhập tràng they read the counter instruction while already under the clock, which on later/faster nights can be a death before the toast is even finished (toast dwell for COUNTER_ keys is floored at 5.5s, hud.gd:422, longer than the 6s attack window is comfortable).
- **Change:** Move the first-contact COUNTER_<id> teach earlier in the approach: emit it from _enter_location/APPROACHING (threat_base.gd:140, when phase becomes APPROACHING and _adjacent_to_door is true) for first contact, instead of only at _arrive_at_door. Plumb a controller hook (e.g. on_threat_approaching) parallel to the existing threat_at_door wiring, gated by the same _first_door dict so it still fires once per night per threat.
- **Risk note:** Touches threat_base approach flow and adds an Events hook; must not alter the kill timer or door gating — verify SelfTest 103/0 still passes since it exercises threat progression.
- **Verify:** NW_SKIP_TAPE=1 NW_NIGHT=6 godot --headless --path . res://tools/SelfTest.tscn must still report 103/0; manually confirm the counter hint precedes the door arrival
- **Verifier:** The finding is accurate. Lines 286-288 of night_controller.gd confirm: COUNTER_ONG_KE / COUNTER_QUY_NHAP_TRANG are emitted only when the threat's `threat_at_door` event fires (i.e., the moment `_arrive_at_door` is called), which is simultaneously with the attack timer starting. The ong_ke attack window is 7s, quy_nhap_trang is 6s; COUNTER_ toasts have a 5.5s floor (hud.gd:422-423). The contrast with meter threats is real: co_hon.gd:23-27 teaches at crowd > 60 (well before kill threat) and ma_da.gd:43-47 teaches at flood > 70 (before 100% kill), giving genuine lead time. Path threats get no equivalent earlier cue.

The proposed fix is sound in principle but the implementation sketch is …

### 28. Offering confirmation does not tell the player WHICH pressure it just relieved, despite affecting three different threats
`[polish | real | safe | P]`  risk=low  conf=high  **scripts/systems/night_controller.gd:913-925 (request_offering), strings.csv OFFERING_DONE**

- **Problem:** One offering simultaneously eases the crowd, the flood, and the grievance (co_hon.gd:41, ma_da.gd:95, oan_hon on_offering) plus adds 14 vía (line 922). The confirmation toast OFFERING_DONE is generic ('Offering placed — the crowd, the water, and the grievance ease.'). With three separate conditional meters on the HUD (HUD_CROWD/WATER/GRIEVANCE, hud.gd:105-107), the player gets a confirmation but the meters drop without a tight visual link to the action, making the cause→effect of a scarce resource harder to learn. The _set_bar flash (hud.gd:241-245) only flashes on DROPS>2 as damage-red, so a beneficial drop reads identically to a hit.
- **Change:** Differentiate beneficial meter drops from damage: in hud._set_bar, when a tracked meter falls right after an offering, flash it green/calm rather than red, OR pulse the three meter rows briefly on Events.offering_placed so the player sees exactly which pressures the offering relieved. Keep OFFERING_DONE as-is.
- **Risk note:** HUD feedback polish only; no economy or threat numbers change.
- **Verify:** manual playtest: place an offering with crowd/water/grievance bars visible, confirm their drop reads as relief not damage
- **Verifier:** All cited facts verified. night_controller.gd:913-925 confirms one offering emits offering_placed, calls broadcast_offering (which invokes on_offering on all active threats), and adds 14 vía. co_hon.gd:41-42 drops crowd by 30, ma_da.gd:95-96 drops flood by 22, oan_hon.gd:57-58 drops agro by 30. strings.csv line 124 OFFERING_DONE reads exactly "Offering placed — the crowd, the water, and the grievance ease." hud.gd:241-245 flashes red (Color 1.4, 0.7, 0.7) on any drop > 2.0 with no distinction between damage and beneficial drops. The offering_placed signal in hud.gd (line 320) is wired only to _refresh_offerings(), not to any meter visual feedback. The gap is real: a player who places an …

### 29. Hourly progress is under-celebrated relative to the 6-hour goal — clock pulse is the only beat, no sense of 'X hours survived'
`[polish | real | safe | P]`  risk=low  conf=high  **scripts/ui/hud.gd:364-375 (_on_hour_reached clock pulse), 632-637 (hour_reached + chime)**

- **Problem:** Surviving each in-game hour is the core progress loop (it pays coins, night_controller.gd:637) but the only feedback is a clock-chime sound and a brief gold pulse of the clock label (hud.gd:364-375). There is no readable sense of 'how far through the night am I' beyond reading the clock face — no progress bar toward 6 AM, no 'Hour 3 — hold on' beat. For a survival loop whose entire win condition is endurance, the dead-air between hourly chimes lacks a pacing anchor, which playtesters commonly report as the night 'dragging' in the calm stretches.
- **Change:** Add a lightweight night-progress indicator: either a thin progress bar under the clock filling toward 06:00 (drive from night_progress(), already exposed at night_controller.gd:1162), or a short '_n_ / 6' survived counter that ticks on hour_reached. Low-risk, reuses existing clock_advanced/hour_reached signals already wired in hud._connect (lines 295-296).
- **Risk note:** Additive HUD element off existing signals; no timing or balance change.
- **Verify:** manual playtest: confirm the player can read at-a-glance how close to 6 AM they are during a calm stretch
- **Verifier:** Finding is real. Confirmed: hud.gd has no night-progress bar or hour counter — only a brief gold clock pulse in _on_hour_reached (lines 364-375). The second cited location (hud.gd:632-637) is a wrong-file error; those lines belong to night_controller.gd:634-637 where the chime and coin earn live. The underlying observation is accurate regardless.

Proposed change is safe: _on_hour_reached already receives the hour int (0-6), so an 'X/6' counter can be appended to the clock vbox with zero new signals. If a progress bar driven by game_minutes is preferred, _c.night_progress() is accessible via the existing controller reference, callable from _refresh_clock which fires every in-game minute. No …

### 30. Offering (F) — the cô hồn / ma da counter — is never taught; players discover it only by reading the help panel
`[invalid | REJECTED | care | P]`  risk=low  conf=high  **scripts/systems/night_controller.gd:436 (TUT_STEPS), 913-925 (request_offering)**

- **Problem:** The Night-1 hands-on tutorial teaches LOOK / INCENSE / BELL / DOOR / CAM (TUT_STEPS at line 436) but never the offering action. Yet 'lay out an offering (F)' is the ONLY counter for Cô hồn (co_hon.gd:13 counter_offering) and a major reliever for Ma da (ma_da.gd:95) and Oan hồn. The COUNTER_CO_HON toast (fired at co_hon.gd:27 when crowd>60) tells the player to press F, but F was never introduced as a control, so a new player on the night Cô hồn appears sees a smothering overlay and an instruction to press a key they have never used. Offerings are also a finite resource (request_offering line 916), making blind first use costly. This is the single biggest onboarding gap on the core-counter axis.
- **Change:** Add an offering beat to the Night-1 lesson: append 'TUT_OFFERING' to TUT_STEPS and a matching _check_tutorial case (done = an offering was placed, e.g. set a _tut_offering flag in request_offering like _tut_incense/_tut_bell at lines 1296/1328). Add the TUT_OFFERING string (vi+en) to strings.csv and re-import the .translation. Alternatively, the lower-friction fix: on the FIRST night Cô hồn is on the roster, fire a one-shot 'press F to feed them' primer toast a few seconds into the night (mirror the existing INVEST_GOAL timer at lines 383-386).
- **Risk note:** Additive tutorial step / one-shot toast; gated to story nights, does not touch invariants or the headless NW_SKIP_TAPE path which skips the tutorial entirely.
- **Verify:** manual playtest: start a fresh save, reach the night Cô hồn appears, confirm the player has been shown the offering control before the crowd meter forces it
- **Verifier:** The gap in TUT_STEPS is real but the finding is invalid as stated because the in-situ mitigation is already implemented. Co hon first appears Night 3 (game_state.gd line 25), two nights after the Night-1 tutorial. Night 1 sets offerings_start = 0 (game_state.gd line 73) since meter == 0 — the player physically cannot place an offering on Night 1, so adding TUT_OFFERING to TUT_STEPS would softlock the tutorial (the step could never complete). The proposed primary fix is therefore unsound. More critically, co_hon.gd lines 8 and 24-27 already implement the finding's own "lower-friction alternative": the _taught flag fires a one-shot COUNTER_CO_HON toast when crowd first crosses 60, before the …

## Game logic & balance  (8)

### 31. Oan hồn 'ignoring too long' barely raises grievance — half the paradox is dead until late nights
`[major | real | safe | P]`  risk=low  conf=high  **scripts/threats/oan_hon.gd:27-29**

- **Problem:** The design (file header, lines 2-4) is a paradox: watching too long AND ignoring too long both raise agro, so the safe play is brief glances. But the ignored-branch gain is `(0.9 + lvl*0.12)*delta*mult` while every frame also subtracts the unconditional settle `agro = clampf(agro - 0.6*delta, ...)`. When the altar is tended, mult is 0.5, so the net ignored gain is `0.9*0.5 - 0.6 = -0.15/s` at low lvl and only barely positive even at N6 (lvl≈6: `(0.9+0.72)*0.5 - 0.6 = +0.21/s`). For most of the campaign the 'ignore her too long' threat does not exist — grievance only ever climbs if you actively watch her, collapsing the intended two-sided knife-edge into a one-sided 'just never look at her' counter. The `_taught`/bleed at agro>80 (lines 37-42) almost never triggers from neglect.
- **Change:** Decouple the settle from the ignored branch: apply the 0.6/s settle ONLY when neither watched nor neglected, i.e. gate it so a genuinely-ignored Oan hồn nets positive. Concretely, raise the ignored base so it clears settle*mult-adjusted: change line 28 ignored gain `0.9 + lvl*0.12` -> `1.6 + lvl*0.18`, OR only apply the -0.6 settle when `agro` is in a mid 'glanced recently' window rather than every frame. Was: ignored gain 0.9+lvl*0.12. Now: 1.6+lvl*0.18 (net positive ~+0.2..+0.5/s ignored even while incensed). One-line revertible.
- **Risk note:** Pure constant tweak in one threat; SelfTest only asserts agro rises while WATCHED (line 162) and offering lowers it (165), neither of which this touches.
- **Verify:** Headless: run SelfTest (must stay 103/0) to confirm no regression. Balance itself needs playtesting — confirm that fully ignoring Oan hồn on N3-6 eventually pushes agro past 80 and bleeds vía, restoring the two-sided pressure.
- **Verifier:** Finding is real and math is correct. Lines 25-29 of /scripts/threats/oan_hon.gd confirm: the settle (-0.6*delta) is unconditional and applies on the same frame as the ignored-branch gain. When the altar is well-tended (huong >= 60, _bright_altar=false), meter_mult() returns 0.5, so the ignored-branch net at lvl=0 is (0.9*0.5) - 0.6 = -0.15/s. Agro actively falls when Oan Hon is ignored and the altar is lit — the exact state competent players maintain. The paradox only functions when the altar is unlit (mult=1.5, net=+0.75/s), but the game tutorializes lighting incense, so the two-sided knife-edge is broken for most sessions. Code comment on line 29 ("slow settle if you neither stare nor …

### 32. Cô hồn bleed has a hard 2.2/s discontinuity at crowd 85 — harmless then suddenly lethal
`[minor | real | safe | P]`  risk=low  conf=high  **scripts/threats/co_hon.gd:29-30**

- **Problem:** Bleed = `(crowd-60)*coef` with `coef = 0.05 if crowd<=85 else 0.11`. At crowd 85 the bleed is `25*0.05 = 1.25/s`; one frame later at 85.001 it jumps to `25*0.11 = 2.75/s` — a 2.2/s step discontinuity. Below 85 the bleed (≤1.25/s) is fully cancelled by vía regen (1.0..2.8/s, night_controller.gd line 701), so the crowd is effectively harmless up to 85, then abruptly out-paces regen. The pressure curve is a cliff, not a ramp: players get no felt warning that they are approaching the lethal zone, and the telegraph at 70 (lines 34-39) fires while the crowd still cannot actually hurt them.
- **Change:** Make the coefficient continuous: replace the two-branch `coef` with a single ramp, e.g. `var bleed := (crowd-60.0)*0.05 + maxf(0.0, crowd-85.0)*0.06`. Same endpoints (1.25/s at 85, ~2.75/s at 100) but no step, so the bleed begins to bite gradually from ~80 where regen starts losing. One-line revertible.
- **Risk note:** Single arithmetic line; SelfTest only checks offering/incense lower crowd (lines 151,154), not the bleed magnitude.
- **Verify:** Headless SelfTest stays green. Smoothness/balance is a feel change — playtest that neglecting Cô hồn produces escalating (not binary) vía loss.
- **Verifier:** Confirmed real. co_hon.gd line 29 has `coef := 0.05 if crowd <= 85.0 else 0.11`, producing a hard step at exactly crowd 85. At crowd 85 the bleed is 1.25/s; one increment above 85 it jumps to ~2.75/s — a ~1.5/s discontinuity (the finding's "2.2/s" figure is slightly off but the qualitative issue is accurate). night_controller.gd line 701 confirms regen = 1.0 + night_progress()*1.8, so below 85 the bleed is always beatable by regen, and the cliff is real. The proposed one-line fix `var bleed := (crowd-60.0)*0.05 + maxf(0.0, crowd-85.0)*0.06` preserves exact endpoints (1.25/s at 85, ~2.75/s at 100) with no step. No strings touched, no localization impact, no cultural/folklore content …

### 33. Ma da flood kill-warning can be permanently suppressed if flood oscillates in the 70-85 band
`[minor | real | safe | P]`  risk=low  conf=high  **scripts/threats/ma_da.gd:51-55**

- **Problem:** The one-shot rising-water telegraph sets `_warned=true` at flood>=85 and only re-arms (`_warned=false`) when `flood < 70`. An offering removes 22 (line 96) and close_drain removes 35 (line 92), so a player who lets flood reach 100, eats the ward/answer, then keeps it managed between ~75 and 99 never drops below 70 — the 'water is rising' cue and COUNTER_MA_DA prompt fire exactly once for the whole night and are silent on every subsequent approach to the 100 kill. The kill can then land with no telegraph.
- **Change:** Lower the re-arm threshold to match the warn hysteresis used elsewhere, or arm per kill-cycle: change line 55 `elif flood < 70.0:` -> `elif flood < 80.0:` so a single offering (−22 from ~99) re-arms the warning before the next rise. Was: re-arm at <70. Now: re-arm at <80. One-line revertible.
- **Risk note:** Only affects an audio/notify telegraph, never the kill math; no SelfTest assertion touches `_warned`.
- **Verify:** Playtest N2+: drive flood to 100, survive via ward, manage it in the 80s, confirm the rising-water cue re-fires before each subsequent near-kill.
- **Verifier:** The finding is real and accurately describes the code at lines 51-55 of scripts/threats/ma_da.gd.

Verification of the logic:
- Line 51-54: `_warned` is set to true when flood >= 85. This fires the "water_call" SFX and COUNTER_MA_DA notify.
- Line 55-56: `_warned` is reset to false only when flood < 70.

The scenario described is valid. With these flood deltas:
- close_drain subtracts 35 (line 92), so from 99 -> 64, which does re-arm below 70. BUT this requires a drain action.
- on_offering subtracts 22 (line 96), so from 99 -> 77, which stays above 70 — warning never re-arms.
- on_calm subtracts 30 (line 99), from 99 -> 69, which barely re-arms.
- on_ward_save caps to 25 (line 102), which …

### 34. Blackout vía bleed resets the burst-drain cooldown every frame, stalling regen for 0.7s after power returns
`[minor | real | safe | H]`  risk=low  conf=high  **scripts/systems/night_controller.gd:704-705**

- **Problem:** In `_update_via`, the unpowered bleed uses `add_via(-2.0*delta)` (line 705). `add_via` with a negative amount sets `_via_drain_cd = 0.7` and multiplies by `via_drain_mult` (lines 1166-1168). During a blackout this re-arms the 0.7s regen-freeze every single frame, and because it is a *burst* drain it is additionally scaled by any taint (via_drain_mult 1.6). The blackout bleed is meant to be a slow continuous pressure (like the altar bleed), but routing it through the burst path means: (a) regen stays frozen for 0.7s after power is restored, and (b) it is amplified by the bánh-lạ taint, which is inconsistent with the comment at lines 698-699 describing meter pressure as offsettable.
- **Change:** Route the blackout bleed through the continuous channel that does NOT freeze regen and is not taint-scaled: change line 705 `add_via(-2.0 * delta)` -> `add_via_drain(-2.0 * delta)`. This matches how the guttered-altar bleed is applied (line 1262). One-line revertible.
- **Risk note:** `add_via_drain` exists and is the documented continuous-drain path; SelfTest's vía-drain section (lines 311-318) checks add_via_drain does NOT set the cd, which this aligns with.
- **Verify:** Headless: in SelfTest set `_powered=false`, call `_update_via(0.1)`, assert `_via_drain_cd <= 0.0` (regen not frozen) and via decreased. Existing power-out test (line 206) still passes.
- **Verifier:** Confirmed real. Line 705 calls add_via(-2.0 * delta), which at line 1168 sets _via_drain_cd = 0.7 on every negative call. During a blackout this runs every frame, permanently re-arming the cooldown, so regen is frozen for the entire blackout plus 0.7s after power returns. The add_via_drain function (line 1403) already exists, applies via_drain_mult the same way, but omits the _via_drain_cd assignment — exactly what the altar bleed comment at line 1259 says is intentional ("so vía regen can still fight it, no hard lockout"). The taint scaling (via_drain_mult) behaves identically in both paths, so no gameplay difference there. Proposed fix (change line 705 to add_via_drain(-2.0 * delta)) is a …

### 35. Ma trơi surge resets lock to exactly 45.0 while the bleed threshold is 40 — a re-locked wisp bleeds vía with no agitation
`[minor | real | safe | H]`  risk=low  conf=high  **scripts/threats/ma_troi.gd:28-29, 58**

- **Problem:** After a surge, `lock` is set to 45.0 (line 58). The vía bleed runs whenever `lock > 40.0` (line 28): `(lock-40)*0.05`. So immediately post-surge the wisp bleeds `(45-40)*0.05 = 0.25/s` even though the player has stopped panning (agit<=0.4). Lock only decays at 8.0/s toward 0, so for ~0.6s after every surge the player is bled despite having correctly calmed down — the surge's own reset value sits inside its own bleed zone. Minor, but it means 'stop running' does not fully stop the bleed for a moment after each lock-on, slightly contradicting the 'don't run is always an out' design (night_controller.gd lines 534-536).
- **Change:** Reset lock below the bleed threshold so a calmed wisp is immediately harmless: change line 58 `lock = 45.0` -> `lock = 38.0` (still above the `_warned` re-arm at <45 handled by the explicit `_warned=false` on line 59, so back-to-back lock-ons still warn). Was: 45.0. Now: 38.0. One-line revertible.
- **Risk note:** SelfTest asserts surge leaves `lock <= 60.0` and `_active` (line 174); 38.0 satisfies it. `_warned` re-arm is forced explicitly on line 59 so lowering below 45 changes nothing there.
- **Verify:** Headless SelfTest line 174 still passes. Optionally assert post-surge `mt.lock <= 40.0` so a stopped player has zero bleed.
- **Verifier:** Verified against actual code. Line 58 sets lock = 45.0 post-surge; line 28 bleeds when lock > 40.0. Math checks out: 0.625s of 0.25/s bleed after every surge even when player is calm. The proposed change to 38.0 is safe: (1) bleed stops immediately since 38 < 40; (2) _warned re-arm is fine because line 59 explicitly sets _warned=false unconditionally in _surge(), so the line 36 elif condition is irrelevant; (3) current_texture() on line 62 checks lock > 45.0, so at 38.0 it already showed tex_idle (identical to 45.0 behavior since 45 is not strictly greater than 45); (4) no documented invariants, cultural rules, localization strings, or display code touched. The only behavioral change is …

### 36. Bell and tag/anomaly setbacks silently no-op against the risen corpse (MODEL_CREEPER) — only the door counters it
`[minor | real | safe | P]`  risk=medium  conf=high  **scripts/systems/threat_director.gd:65-66**

- **Problem:** `nearest_threat` (used by the bell via `setback_nearest`, night_controller.gd line 1333) skips any threat whose `movement_model` is not MODEL_PATH or MODEL_FLYER. After Quỷ nhập tràng triggers, it switches to MODEL_CREEPER (quy_nhap_trang.gd line 63), so the bell — the player's emergency 'shove the nearest rusher back' tool — does nothing against the corpse actively rushing the right door, while still consuming its cooldown and granting vía/reveal. A player reflexively ringing the bell when the corpse looms gets no setback and may not realize only the right door works. The corpse is the most dangerous rusher precisely when the bell becomes a no-op.
- **Change:** Either (a) include MODEL_CREEPER in the `nearest_threat` eligibility so the bell can set the corpse back one node (it still must be re-shut), changing line 65 to also permit MODEL_CREEPER; or (b) leave the mechanic but surface it: when `setback_nearest` finds no eligible target, emit a distinct notify so the bell's no-op is legible. Option (a) is the one-line gameplay fix; risk that it makes the corpse too easy, so (b) is the safer UX-only change.
- **Risk note:** Option (a) changes Quỷ nhập tràng difficulty (it is intentionally door-only); prefer (b) unless a setback is desired. No SelfTest asserts on nearest_threat eligibility.
- **Verify:** Playtest N4-6: trigger the corpse, ring the bell while it rushes, confirm intended behavior (either a one-node setback or a clear 'no effect' cue). Headless SelfTest stays green either way.
- **Verifier:** Confirmed real. In threat_director.gd:65-66, `nearest_threat()` filters to MODEL_PATH and MODEL_FLYER only. After `_trigger()` in quy_nhap_trang.gd:64 sets `movement_model = MODEL_CREEPER`, the risen corpse is silently excluded from bell's `setback_nearest()`, consuming the bell cooldown and granting via (night_controller.gd:1326-1334) while doing nothing. `counter_door` stays `true` (quy_nhap_trang.gd:24, never changed in `_trigger()`), so the corpse would pass the second filter — the movement model check alone is the blocker. Minor correction to the finding: `tag_anomaly()` (night_controller.gd:1373) calls `t.reset_to_spawn()` directly via `counter_door`, bypassing `nearest_threat()`, so …

### 37. Difficulty level-scale rounds introduced threats down by up to a full tier on EASY, flattening the intended ramp
`[minor | real | safe | P]`  risk=low  conf=high  **scripts/core/game_state.gd:82-83**

- **Problem:** `scaled[id] = clampi(int(round(level * lvl_scale)), 1, 20)` with EASY scale 0.7. The story table introduces threats at low integer levels so each mechanic is learned (e.g. N2 ma_da=2, N3 co_hon=3). On EASY: ma_da 2->round(1.4)=1, co_hon 3->round(2.1)=2, quy_nhap_trang(N4)=3->round(2.1)=2. The floor-at-1 (comment lines 80-82) preserves presence, but several newly-introduced spirits land at level 1-2 where their per-frame growth (`0.35 + lvl*0.13` etc.) is so low they barely act for the whole introductory night — the night that is supposed to teach that spirit. The ramp from N1->N6 on EASY is noticeably flatter than intended, and the teaching night for each mechanic is the one most weakened.
- **Change:** Apply difficulty scaling additively or with a higher floor for introduced threats so a spirit's debut night keeps it at a teachable level, e.g. floor introduced threats at 2 instead of 1, or scale toward a baseline: `scaled[id] = clampi(int(round(maxf(level*lvl_scale, level-2.0))), 2, 20)`. Simplest revertible form: raise the clamp floor from 1 to 2 on line 83. Was: clamp floor 1. Now: 2.
- **Risk note:** Affects only EASY (and rounding edge cases); NORMAL/HARD/NIGHTMARE scales (1.0/1.25/1.6) round up and are unaffected. No SelfTest pins exact scaled levels (it runs NW_NIGHT=6 at default difficulty).
- **Verify:** Headless: instantiate `_build_config` with EASY and assert each introduced threat scales to >=2. Feel/ramp needs an EASY playthrough.
- **Verifier:** Finding is real and the math checks out. Verified in scripts/core/game_state.gd line 83: `scaled[id] = clampi(int(round(float(levels[id]) * lvl_scale)), 1, 20)` with EASY scale 0.7. Debut levels: ma_da N2=2→1, co_hon N3=3→2, quy_nhap_trang N4=3→2. These are exactly the introduction nights the STORY table is designed around. The comment on lines 81-82 explicitly states the floor is meant to preserve "clue-critical or introduced spirits" but the floor of 1 does not meet that stated intent for teachability. Minor inaccuracy in the audit: ma_da growth is `0.35 + lvl*0.11` (not 0.13 as claimed), but the qualitative argument is unchanged. Proposed change (raise clamp floor 1→2) is safe: …

### 38. Pure meter threats are never 'looming', so vía regen always fights them — director.threat_at_door check can't see meter pressure
`[polish | real | care | P]`  risk=medium  conf=high  **scripts/systems/night_controller.gd:696-702**

- **Problem:** Regen is gated on `not looming`, where `looming = director.threat_at_door(LEFT/RIGHT) != null`. Meter threats (Cô hồn, Ma da, Oan hồn, Ma trơi) never enter phase AT_DOOR, so `threat_at_door` always returns null for them and regen (up to 2.8/s late-night) runs at full rate against their bleed. Combined with the modest meter bleeds (Finding: Cô hồn ≤1.25/s below 85; Ma da `(flood-70)*0.05` ≤1.5/s; Oan hồn `(agro-80)*0.06` ≤1.2/s), this means below their top bands NONE of the four meter spirits can net any vía loss — they are pure telegraph until they hit the extreme tail. The comment (lines 698-699) intends regen to 'offset' meter pressure, but in practice it fully cancels it for most of each meter's range, making the spirits feel toothless until the very edge.
- **Change:** Gate regen on actual active pressure, not just door presence. Add a controller-side 'meter danger' check, e.g. regen only when `not looming and _via_drain_cd <= 0.0 and not _any_meter_pressing()`, where `_any_meter_pressing()` returns true if any threat's meter is in its bleed band (e.g. crowd>60 / flood>70 / agro>80 / lock>40). Cheapest concrete form: have meter threats route their bleed through `add_via` (which sets `_via_drain_cd`) instead of `add_via_drain` during their HIGH band only, so active bleed briefly pauses regen — keeping the low-band 'offsettable' design but letting the high band actually win. Revert by switching the high-band call back to add_via_drain.
- **Risk note:** Touches the shared regen gate read by every threat; SelfTest line 316 asserts add_via_drain does NOT freeze regen, so the fix must keep low-band drains on add_via_drain and only change high-band behavior.
- **Verify:** Headless SelfTest must stay 103/0 (esp. lines 311-318). Real balance — playtest that neglecting a single meter spirit (e.g. Cô hồn at crowd 90) now produces sustained net vía loss instead of a stalemate with regen.
- **Verifier:** The finding is mechanically accurate. `_bleed_via()` in threat_base.gd line 253-254 calls `add_via_drain()`, which does NOT set `_via_drain_cd` (see night_controller.gd line 1403-1407 vs line 1165-1170 for `add_via`). Regen at line 700 is gated on `_via_drain_cd <= 0.0 and not looming`, and `looming` only checks `threat_at_door` which returns null for pure-meter threats that never enter AT_DOOR. So regen runs freely against all four meter bleeds.

However, the code already explicitly documents this as the intended design. The comment at night_controller.gd line 1401-1402 reads: "Continuous meter drain that does NOT freeze vía regen — meter pressure is a net bleed you can offset, not an …

## Code health  (8)

### 39. Extract the living-altar / incense ritual into a helper class (clean seam)
`[minor | real | care | H]`  risk=medium  conf=high  **scripts/systems/night_controller.gd:467-499, 1240-1352 (plus state vars 52-68)**

- **Problem:** The altar/incense ritual is the single largest cohesive responsibility tangled into NightController: _init_altar (467), _apply_shrine_upgrades (484, altar-related branches), _update_altar (1242), _gutter_candles (1274), request_light_incense (1288), _light_incense (1302), add_nhang (1321), request_ring_bell (1325), meter_mult (1344), backed by ~12 member vars (huong, huong_max, altar_lit, _huong_decay, _bright_altar, _incense_cd, _nhang, _bell_cd, _bell_cd_max, _draft_cd, _auto_relight, _door_drain_mult). This is ~180 lines of self-contained mechanic inflating the 1448-line file and making the night loop hard to read.
- **Change:** Create scripts/systems/altar_ritual.gd (Node) owning huong/altar_lit/_nhang/_draft_cd/_bell_cd/decay/upgrades. Move the listed methods verbatim; expose update(delta), light_incense(), ring_bell(), gutter(), meter_mult(), and getter get_huong(). NightController keeps thin forwarders for the public API threats already call (get_huong, meter_mult, broadcast_calm) and calls altar.update(delta) from _process. Shared state that makes this risky: meter_mult() is read by threats via the controller and add_via_drain()/start_reveal()/director.setback_nearest() are called from inside the ritual, so the helper needs a back-reference to the controller (pass `self` in setup, mirroring item_system.setup). Keep Events.* emits identical so HUD/SelfTest still see huong_changed/incense_changed/altar_lit_changed.
- **Risk note:** Touches the huong meter and vía-drain wiring that SelfTest exercises; a missed Events emit or back-ref call would change behavior.
- **Verify:** Run tools/SelfTest.tscn (NW_SKIP_TAPE=1 NW_NIGHT=6 godot --headless --path . res://tools/SelfTest.tscn) expecting 103/0 and IntegrityCheck.tscn 36/0; both exercise the altar/incense/bell path.
- **Verifier:** The finding accurately describes the code. The altar/incense block (lines 467-499, 1240-1352) plus ~12 member vars at 52-68 is a self-contained mechanic sitting in a 1448-line file. The altar functions do form a cohesive responsibility distinct from the rest of NightController.

However, the proposed change is NOT safe to apply as described, for two concrete reasons:

1. SelfTest directly accesses fields and private methods on NightController by name: `c.huong`, `c.altar_lit`, `c._incense_cd`, `c._nhang`, `c._bell_cd`, `c._gutter_candles()`, `c._light_incense(true)`, `c.meter_mult()`, `c.request_light_incense()`, `c.request_ring_bell()` (selftest.gd lines 277-309). Moving these to a child …

### 40. Duplicated audio-loop teardown block in _caught and _win
`[minor | real | safe | H]`  risk=low  conf=high  **scripts/systems/night_controller.gd:1018-1026 vs 1102-1107**

- **Problem:** Both end states stop the same loops and reset the same five flags. _caught (1018-1026) iterates a literal list of 8 loop names then sets _drone_on/_hb_on/_breath_on/_strain_on/_water_on = false; _win (1102-1107) calls Audio.stop_all_loops() then resets the identical five flags. The flag-reset is copy-pasted and the loop list in _caught duplicates knowledge already centralized in Audio.stop_all_loops(). If a new tension loop is added, two call sites (and the _power_out partial at 681-684) must be updated in lockstep — exactly the kind of divergence that breaks the documented 'audio loop cleanup on death/win' invariant.
- **Change:** Add one private helper, e.g. func _stop_tension_loops() -> void that calls Audio.stop_all_loops() and clears the five _*_on flags. Call it from both _caught and _win. (Do NOT fold in the duck/music handling, which differs between the two — only the loop+flag teardown is shared.) This preserves the POLISH_NOTES invariant while removing the divergence risk.
- **Risk note:** Pure extraction of identical statements; _caught currently stops loops individually but stop_all_loops covers the same set, verifiable headless.
- **Verify:** SelfTest drives a death (and a NW_FASTWIN win); run both harnesses (103/0, 36/0). Optionally assert Audio has no active loops after game_over.emit in a SelfTest check.
- **Verifier:** The duplication is real and confirmed. Lines 1018-1026 in _caught stop 8 named loops individually then reset 5 flags; lines 1102-1107 in _win call Audio.stop_all_loops() then reset the same 5 flags. The flag reset block is a verbatim copy-paste.

One notable asymmetry the finding underplays: _caught uses a literal list of 8 specific loop names while _win calls stop_all_loops() (which stops ALL loops in the _loops dict). These are behaviorally equivalent today but _caught will miss any future loop not in its hardcoded list. Extracting the helper as proposed would also fix this asymmetry by making _caught use stop_all_loops() too.

Severity downgrade from major to minor is warranted: no …

### 41. Dead method show_threat() is never called
`[minor | real | safe | H]`  risk=low  conf=high  **scripts/systems/guard_room.gd:1024-1033**

- **Problem:** show_threat(side, tex, hostile) is fully superseded by refresh_threat_visibility (1035) and has zero call sites in the codebase (grep across scripts/ shows only its own definition and refresh_threat_visibility's two real callers in night_controller.gd:969/971). It duplicates the modulate/visibility logic of refresh_threat_visibility, so it is dead code that also drifts: any future tweak to door-sprite tinting must touch both or silently diverge.
- **Change:** Delete show_threat() (lines 1024-1033). refresh_threat_visibility already covers the has_threat==false case via its visible=has_threat... line. Per CLAUDE.md this is pre-existing dead code, so flag-and-remove only as part of an explicitly-approved cleanup; otherwise leave it and note it.
- **Risk note:** No callers, so removal cannot change runtime behavior; only risk is an external scene referencing it, which grep rules out.
- **Verify:** grep confirms no callers; run SelfTest/IntegrityCheck after removal (103/0, 36/0) to confirm nothing referenced it via has_method.
- **Verifier:** Confirmed: show_threat() at scripts/systems/guard_room.gd:1024-1033 has exactly one occurrence in scripts/ — its own definition. refresh_threat_visibility() is the live replacement, called at night_controller.gd:969 and :971. The two methods share identical modulate logic (hostile ? Color(1,0.45,0.4) : Color(0.72,0.74,0.74)) and the same _light_on visibility gate. Deleting lines 1024-1033 is behavior-preserving (no callers to break), touches no documented invariant, no cultural rule, no localization string, no audio/asset pipeline, and neither SelfTest nor IntegrityCheck harness references it. Per CLAUDE.md the pre-existing dead code should be flagged rather than auto-removed; a standalone …

### 42. Hostile/idle sprite tint colors duplicated as magic literals
`[minor | real | safe | H]`  risk=low  conf=high  **scripts/systems/guard_room.gd:1032 and 1040**

- **Problem:** Color(1.0, 0.45, 0.4) (hostile rim) and Color(0.72, 0.74, 0.74) (idle) are hard-coded identically in both show_threat and refresh_threat_visibility. The two copies must stay in sync by hand; the values carry no name, so the intent (hot rim vs neutral silhouette) lives only in a comment repeated in both spots.
- **Change:** Introduce const THREAT_TINT_HOSTILE := Color(1.0, 0.45, 0.4) and const THREAT_TINT_IDLE := Color(0.72, 0.74, 0.74) near the other color constants (top of file) and reference them. If show_threat is removed (prior finding), only refresh_threat_visibility remains, but the named constants still document intent.
- **Risk note:** Constant substitution with identical values; no behavioral change possible.
- **Verify:** Visual-only effect, but compile + IntegrityCheck (36/0) confirms the script still loads and the constants resolve.
- **Verifier:** Confirmed: Color(1.0, 0.45, 0.4) and Color(0.72, 0.74, 0.74) appear verbatim on both lines 1032 and 1040 in guard_room.gd. No THREAT_TINT_* constants exist anywhere in the file. The file already uses a top-of-file const block for numeric literals (lines 10-103), so the proposed pattern fits. The fix is a pure rename with no logic change — no localization strings touched, no audio/re-entrancy invariants involved, no cultural-rule code nearby, no display/resolution code affected. Both harnesses (SelfTest, IntegrityCheck) are purely headless behavior tests and would be unaffected by renaming these color literals. No risk of breaking any documented invariant.

### 43. Jumpscare/juice flow (_on_jumpscare/_caught/_strobe_flash) is a separable module
`[minor | real | care | H]`  risk=high  conf=high  **scripts/systems/night_controller.gd:974-1094 (plus _build_ui scare setup 219-253, _process jitter 505-514)**

- **Problem:** The jumpscare presentation — building _jumpscare_rect/_scare_mat/_flash (219-253), per-frame jitter decay (505-514), and the 3-phase scare sequence in _caught (992-1084) plus _strobe_flash (1088-1094) — is a large, cohesive, behavior-self-contained chunk threaded through unrelated parts of the controller. It owns its own members (_jumpscare_rect, _scare_mat, _flash, _jump_jitter_t) and its only couplings to the night sim are the Settings.allow_jumpscares() gate, room.add_shake, and Router.to_game_over. Mixing this ~140-line cinematic into the clock/power/vía controller is a top driver of the file's size and a frequent re-entrancy hazard (the _ending guard at 994 is the documented invariant protecting it).
- **Change:** Extract scripts/systems/jumpscare_player.gd (a CanvasLayer/Node) that builds the scare/flash UI and exposes play(cause, fear, progress) plus _process jitter. NightController._caught keeps the _ending/_running/director.set_paused bookkeeping, the coin/best logic, and Events.game_over.emit, then awaits jumpscare_player.play(...) and calls Router.to_game_over. CRITICAL: preserve the _ending early-return and the Settings.allow_jumpscares() gating (jump image, shake, strobe must all stay behind it) per the binding cultural + POLISH rules. Shared-state risk: the await timers in _caught currently re-check is_inside_tree(); the helper must keep those guards so a scene-change mid-scare cleaks nothing.
- **Risk note:** Touches the scare-OFF accessibility gate and the death-sequence awaits; a regression here violates a binding cultural constraint, not just code health.
- **Verify:** SelfTest runs with scares effectively skipped headless and asserts the death/route path; run 103/0. Add/keep a check that with Settings.scare OFF no shake/flash fires. Full visual confirmation of the scare itself needs human playtesting.
- **Verifier:** The jumpscare block is real and exactly as described: UI construction (219-253), jitter decay in _process (505-514), 3-phase scare sequence (992-1084), and _strobe_flash (1088-1094) are cohesive cinematic logic sitting in a 1448-line orchestration file. However, the current code is not broken — _ending at line 994 correctly guards re-entrancy. Severity downgraded from major to minor because this is a pure refactor of working code. The proposed extraction carries three concrete risks not adequately flagged: (1) The two `await get_tree().create_timer(...).timeout` calls in _caught use `is_inside_tree()` guards keyed to NightController — if those awaits move to a helper node, the guards must …

### 44. Per-night flavor-mod flags duplicate config.mods string keys in scattered checks
`[minor | real | safe | H]`  risk=low  conf=high  **scripts/systems/night_controller.gd:92-96, 389-414, 530-531**

- **Problem:** The mod system caches _mod_blackout/_mod_fog/_mod_wisp from config.mods.has("blackout"/"fog"/"wisp_storm") in _apply_mods (389), but the magic string keys appear only here while the cached bools are read in _update_mods (401) and _process agitation (530). The blackout cadence numbers randf_range(45.0, 75.0) are also written twice (397 and 409) and the window length 9.0/14.0 once — these timing magic numbers are uncommented and split across functions, so tuning the blackout cadence means editing two literals that must match.
- **Change:** Hoist the blackout cadence to named consts (e.g. const BLACKOUT_GAP := Vector2(45.0, 75.0), const BLACKOUT_LEN := Vector2(9.0, 14.0)) and use them at both 397 and 409, and define the mod keys as consts (MOD_BLACKOUT := "blackout", etc.) so the string literals have a single source. Low-scope, no behavior change.
- **Risk note:** Replacing duplicated literals with shared consts of identical value cannot change the sim.
- **Verify:** Compile + IntegrityCheck (36/0); SelfTest on a blackout night (NW_NIGHT with the mod) stays 103/0.
- **Verifier:** Finding is real but slightly overstated on the string-scatter claim. The mod string keys ("blackout", "fog", "wisp_storm") appear only once in night_controller.gd (in _apply_mods at lines 391-393), so they are not "scattered across checks" within the file — however they do also appear as raw string literals in game_state.gd config data, meaning a rename would require two-file edits with no compile-time safety. The duplicated magic numbers are accurately identified: randf_range(45.0, 75.0) appears at both line 397 (initial cooldown seed) and line 409 (reset after window fires), and tuning cadence requires editing both. The proposed fix — named consts BLACKOUT_GAP/BLACKOUT_LEN and …

### 45. Room-building is a 350-line block that can split build vs animate
`[polish | real | care | H]`  risk=high  conf=high  **scripts/systems/guard_room.gd:261-736 (build) vs 739-849 (animate)**

- **Problem:** guard_room.gd mixes two distinct concerns at file scale: one-time procedural construction (_build_room 261, _build_monitor_screen 293, _build_dvr 347, _build_desk_lamp 368, _build_altar 390, _build_chair 517, _build_props 533, _build_ceiling_fan 593, _build_apparition 606, _build_window 655, _build_doorway 678 — ~470 lines) and per-frame animation/look/dread (739-1042). The build half is pure static geometry that never touches runtime state except storing handles in members. This is the cleanest decomposition seam in the file: the mesh helpers (_mat 199, _emat 216, _box 224, _cyl 234, _sphere 248) plus the _build_* methods could move to a RoomBuilder helper that returns the node handles guard_room caches.
- **Change:** Extract scripts/systems/room_builder.gd holding the geometry helpers and _build_* methods, called once from _ready; it populates and returns a small struct/dictionary of the handles guard_room animates (_candles, _smoke, _ceiling, _tube_mat, _desk_lamp(_mat), _altar_light, _fan, _hand_*, _doors, _lights, _lamp_mats, _threat_sprites, _screen_mat, _feed_by_cam). guard_room.gd keeps only the look/shake/dread/animate/public-API surface. Shared-state risk: many members are written by build and read by animate, so the handoff dictionary must cover every one — easy to miss _incense_tips or _threat_base_pos. Lower-effort alternative if a full extract is too risky: at minimum split _build_altar's runtime-handle bookkeeping from its static geometry. Note: this is procedural-pipeline work only, allowed under the placeholder-asset constraint.
- **Risk note:** ~20 cross-referenced member handles must be threaded through the handoff; a single missed reference yields a null at animate time only on certain nights.
- **Verify:** IntegrityCheck.tscn (36/0) builds the room and checks node structure; SelfTest (103/0) runs a full night driving doors/lights/altar/screen. Both must stay green after the extract.
- **Verifier:** The structural observation is accurate: guard_room.gd is 1041 lines, with build methods spanning roughly lines 129–736 and animate/look/API spanning 739–1042. The two concerns are genuinely present in the same file. However, the "major" severity is wrong for this codebase and context.

Why not safe to apply: The finding itself flags the central risk — "easy to miss _incense_tips or _threat_base_pos." There are at least 15 member variables that bridge build and animate phases (_candles, _smoke, _incense_tips, _ceiling, _tube_mat, _desk_lamp, _desk_lamp_mat, _altar_light, _altar_mat, _fan, _hand_min, _hand_hr, _screen_mat, _feed_by_cam, _threat_sprites, _threat_base_pos, _doors, _lights, …

### 46. Weak/missing type hints on Tween and director-threat locals invite silent nil bugs
`[invalid | REJECTED | care | H]`  risk=low  conf=high  **scripts/systems/night_controller.gd:1007-1008, 1040-1042, 983, 967**

- **Problem:** Several gameplay-critical locals are untyped or rely on Variant: `var t := director.get_threat(cause)` (1007) then `float(t.fear_factor)` guarded by a ternary, and `var t := director.get_threat(threat_id)` (983) used without an explicit type; meta lookups `var meta := ThreatRegistry.info(cause)` (1040) and `scare_path` (1041) are Variant-typed via .get(). Because director.get_threat can return null and the threats are duck-typed (room/vendor/director are declared as untyped `var`, e.g. lines 10/16/17), the static analyzer cannot catch a wrong member access; these become runtime errors only on specific threats. This is the same untyped-handle pattern as room/vendor/item_system at the top of the file.
- **Change:** Add explicit return-type hints to the duck-typed system getters where a class exists (director is already ThreatDirector-typed; type room/vendor/item_system/phone with their class_name if one exists, or annotate the local `var t: ThreatBase = director.get_threat(...)`). At minimum annotate the get_threat locals as the threat base type so member access (fear_factor, reset_to_spawn, on_ward_save) is statically checked. Keep the existing null-guards.
- **Risk note:** Adding hints to locals that already match their runtime type is non-behavioral; only risk is if a returned object lacks the annotated type, which the existing guards already handle.
- **Verify:** godot --headless --check-only (or simply loading the scene) surfaces type-mismatch parse errors; then SelfTest 103/0 and IntegrityCheck 36/0 confirm no regression.
- **Verifier:** The finding is factually wrong. All cited locals are already statically typed via Godot's type inference through := with annotated callees. (1) `director` is declared `var director: ThreatDirector` (line 11), and `ThreatDirector.get_threat` is annotated `-> ThreatBase` (line 47 of threat_director.gd), so `var t := director.get_threat(...)` at lines 983 and 1007 gives `t` the static type `ThreatBase` — the analyzer can and does check `.fear_factor`, `.reset_to_spawn()`, `.on_ward_save()` against ThreatBase. (2) `director.threat_at_door` is likewise annotated `-> ThreatBase`, so `t` at line 967 is `ThreatBase`. (3) `ThreatRegistry.info` is annotated `-> Dictionary` (line 66 of …
